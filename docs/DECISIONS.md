# Decision Log

Append-only. Never delete a decision. If one changes, add a new entry that supersedes the old one and mark the old one **Superseded by D-NNN**.

Format: `D-NNN · date · title`: decision, why, alternatives considered.

---

### D-001 · 2026-09-22 · Rewrite from scratch
**Decision:** Archive the prototype (tag `legacy-prototype`, branch `legacy/prototype`) and rebuild on a new layout.
**Why:** Its tangled auth, parallel tunnel models, tokens in the webview, and oversized views made fixing it more expensive than rewriting it.
**Alternatives:** An incremental refactor was rejected because nearly every file needed replacing.

### D-002 · 2026-09-22 · macOS first
**Decision:** macOS UX polish comes first through v1.0. Linux and Windows must compile and pass CI from M0, and are polished in M7/M8.
**Why:** It's the maintainer's platform, where "native" is most visible, and focus ships faster.

### D-003 · 2026-09-22 · Route-centric product model
**Decision:** The primary noun is **Route** (hostname → local service). Tunnels are auto-provisioned (one per machine by default) and shown in an Advanced view.
**Why:** Users think in hostnames. Multi-domain on one machine is simply N routes on one tunnel.

### D-004 · 2026-09-22 · Plan → apply engine for every mutation
**Decision:** All mutations are Intents, planned against an observed snapshot, previewed, executed with activity logging and rollback, then verified.
**Why:** One code path powers previews, drift handling, cleanup, Doctor fixes and imports, and prevents half-applied changes.

### D-005 · 2026-09-22 · Cloudflare is the source of truth
**Decision:** Tunnel config and DNS are read from Cloudflare. SQLite stores only ownership, run modes, ports, applied versions, activity and preferences.
**Why:** It's safe with multiple machines and dashboard edits. Drift is detected through the config `version`.

### D-006 · 2026-09-22 · Ownership via DNS record comments
**Decision:** Records we create carry `teitunnel:route=<id>`. Only owned records are auto-deleted.
**Why:** It gives zero-mess cleanup without risking user records, and ownership can be rebuilt on a new machine. Comments work on all plans; tags don't.

### D-007 · 2026-09-22 · Minimum cloudflared 2025.6.1
**Decision:** Require cloudflared ≥ 2025.6.1 (`--output json`; `--token-file` needs ≥ 2025.4.0). The managed binary tracks the latest release. There's no text-log parser.
**Why:** Structured logs are reliable, and the managed binary makes the requirement painless.

### D-008 · 2026-09-22 · Status from local endpoints, not log scraping
**Decision:** Use cloudflared's metrics server: `/ready` (health), `/quicktunnel` (Quick Share URL), `/metrics` (telemetry), `/diag` (diagnostics).
**Why:** It's stable and structured, and survives log format changes.

### D-009 · 2026-09-22 · Always-on via OS service manager, no custom daemon
**Decision:** Always-on connectors are launchd agents (then systemd --user and Task Scheduler). The app observes them through fixed metrics ports.
**Why:** Production-grade persistence without writing and securing our own daemon.

### D-010 · 2026-09-22 · Auth: OAuth (PKCE, loopback) first, API token second, cert.pem import third
**Decision:** Register a public Cloudflare OAuth client under the verified teispace.com domain. Use loopback redirects on fixed registered ports, since custom schemes aren't allowed. Ship the token flow first as a fallback.
**Why:** Cloudflare opened self-managed OAuth to all customers in June 2026, and it's the best onboarding UX.

### D-011 · 2026-09-22 · Secrets never cross IPC; run tokens never in argv
**Decision:** See [SECURITY_MODEL.md](SECURITY_MODEL.md).

### D-012 · 2026-09-22 · Workspace layout
**Decision:** A Cargo + pnpm workspace: `crates/{cf-api,cloudflared,core}`, `apps/desktop/{src,src-tauri}`, `tools/fake-cloudflared`. Core logic is Tauri-free.
**Why:** Testability, clear boundaries, and room for a future CLI.

### D-013 · 2026-09-22 · Frontend stack
**Decision:** React 19, TypeScript (strict), Vite, Tailwind v4, shadcn/Radix (re-tokened), TanStack Router + Query + Virtual, Zustand (UI state only), react-hook-form + zod, cmdk, sonner, uPlot, lucide, motion (springs; see D-022). Biome for lint/format.
**Why:** Typed routing and server-state caching remove hand-written sync code. uPlot is roughly 10× lighter than Recharts and suited to realtime use.
**Dropped:** Recharts, xterm.js (terminal removed), qrcode.react (QR is rendered as SVG in Rust).

### D-014 · 2026-09-22 · Typed IPC via tauri-specta
**Decision:** Generate `bindings.ts` from Rust. CI fails on drift.
**Note:** tauri-specta 2.x is still RC (2.0.0-rc.25 as of 2026-09). Pin the exact version. If it becomes a blocker, the fallback is `ts-rs` plus hand-written command wrappers.

### D-015 · 2026-09-22 · Tauri 2 stable, not 3 alpha
**Decision:** Build on Tauri 2.11.x. Re-evaluate once Tauri 3 is stable.

### D-016 · 2026-09-22 · In-app dev gallery instead of Storybook
**Decision:** Primitives and patterns are showcased at a dev-only route inside the real app window.
**Why:** Native fidelity (WKWebView rendering, vibrancy, system accent) can't be judged in a browser Storybook.

### D-017 · 2026-09-22 · E2E testing via WebdriverIO `@wdio/tauri-service`
**Decision:** Use the embedded WebDriver approach, which supports macOS. Playwright is used for component/visual tests against mocked IPC in the WebKit engine.

### D-018 · 2026-09-22 · Bundle identifier `com.teispace.teitunnel`
**Decision:** Use it as the app identifier, keychain service name, launchd label prefix and app data folder name.
**Why:** It matches the teispace.com domain, which is also used for OAuth domain verification.

### D-019 · 2026-09-22 · No telemetry
**Decision:** No analytics, crash upload or phone-home other than update checks, which can be disabled.

### D-021 · 2026-09-22 · Materials: NSVisualEffectView + CSS materials, no NSGlassEffectView (for now)
**Decision:** The sidebar uses native vibrancy via `windowEffects`. Floating toolbar controls use restrained CSS materials. The private `NSGlassEffectView` is not used.
**Why:** It crashes packaged Tauri apps on macOS 27 (tauri-apps/window-vibrancy#229). macOS 27 moved sidebars back to edge-to-edge, which NSVisualEffectView renders natively.
**Revisit:** each milestone, via the issue.

### D-022 · 2026-09-22 · Motion: SwiftUI-style springs
**Decision:** Spring tokens (duration + bounce) are shared by CSS `linear()` easings (simple transitions) and `motion/react` (layout, presence, gestures). Interruptible by default.
**Why:** This matches Apple's motion model. CSS keeps simple transitions free of JS cost.

### D-020 · 2026-09-22 · Remove the embedded terminal
**Decision:** No arbitrary command runner. It's replaced by a structured log viewer and "Copy as command" on every plan step and activity entry.
**Why:** It was a security risk with low value.

### D-023 · 2026-09-22 · Minimum macOS 14, `light-dark()` tokens, accent from AppKit
**Decision:** `minimumSystemVersion` is 14.0 and the web build targets Safari 17. Colour tokens use CSS `light-dark()` and `color-scheme`, so each token is defined once and the theme override only pins `color-scheme`. The accent colour is read from `NSColor.controlAccentColor` (command `app_accent_color`) and applied as `--accent` on start and whenever the window regains focus.
**Why:** Verified on macOS 27: WKWebView resolves CSS `AccentColor` to a fixed blue `(52,120,246)` even when the system accent is green, while System Settings follows it. Reading AppKit gives values identical to System Settings, updated live. `light-dark()` removes a whole duplicated dark block.

### D-024 · 2026-09-22 · Dark sidebar tint over native vibrancy
**Decision:** The sidebar keeps the native `sidebar` NSVisualEffectView, with a dark-mode-only tint `rgb(0 0 0 / 0.42)` (`--surface-sidebar`).
**Why:** Measured on macOS 27: Finder and System Settings sidebars render ~`(38,40,41)` in dark mode, while the NSVisualEffectView `sidebar` material renders ~`(69,70,70)`. The tint lands at `(40,41,41)` and keeps some translucency. Light mode already matches (231 vs 236).

### D-025 · 2026-09-22 · macOS 27 sidebar and title-bar metrics
**Decision:** Sidebar rows are 32 px with 18 px accent-tinted icons, 10 px side inset and 8 px selection radius. Traffic lights use `trafficLightPosition {x: 19, y: 28}`, which puts the buttons at exactly the same pixels as System Settings (close button 19–32.5 pt × 20–31.5 pt). Selection happens on mouse-down, as in NSOutlineView.
**Why:** Measured from System Settings and Finder screenshots on macOS 27. This supersedes the 28 px row / 6 px radius / (18,18) values in the first DESIGN draft.

### D-026 · 2026-09-22 · Dependabot instead of Renovate; no `tauri-plugin-os`
**Decision:** Weekly grouped Dependabot updates (Cargo, npm, Actions), with Tauri majors and specta RCs ignored. The `os` plugin is dropped because `app_info` already reports platform and arch.
**Why:** Dependabot needs no third-party app installation (a maintainer-only action). Fewer plugins means a smaller attack surface.

### D-027 · 2026-09-22 · Respect pnpm's minimum release age
**Decision:** Keep pnpm 12's default supply-chain policy (packages must be at least a day old). Pin the previous release when the latest is too new (e.g. motion 13.4.0, jsdom 30.1.0).
**Why:** It's a cheap defence against compromised fresh releases, consistent with SECURITY_MODEL.

### D-028 · 2026-09-22 · Settings is a separate native window
**Decision:** ⌘, opens a dedicated Settings window (620×460, not resizable, overlay title bar) that loads the `/settings` route, instead of a page inside the main window.
**Why:** That's the macOS convention (every native app's Settings is its own window). It also keeps preferences reachable while the main window is hidden.

### D-029 · 2026-09-22 · Closing the window never stops tunnels; "Show in menu bar" controls the icon
**Decision:** Closing the main window hides it; the app keeps running (Dock icon and menu bar item reopen it) and ⌘Q quits. The setting is `showInMenuBar` (default on), which only toggles the menu bar icon.
**Why:** Matches Mail/Messages behaviour and avoids surprising tunnel shutdowns. The quit confirmation for running Session connectors lands with connectors in M1.

### D-030 · 2026-09-22 · WebKit (Playwright) screenshots when the native window can't be captured
**Decision:** `pnpm --filter @teitunnel/desktop shoot <dir> [routes…]` renders routes in Playwright's WebKit (light and dark) with the measured sidebar material painted in. It's used for visual review in CI-like conditions and when the screen is locked during unattended sessions. Native captures remain the reference for vibrancy, traffic lights and accent.
**Why:** macOS doesn't composite windows while the screen is locked, so `screencapture` returns blank content. Headless WebKit also doesn't render `backdrop-filter`, so materials must stay readable without blur.

### D-031 · 2026-09-22 · Two CSS materials: glass (controls) and panel (popovers, menus, palette, toasts)
**Decision:** `material-glass` (62% tint) is only for small controls floating over content. Panels use `material-panel` (94% tint, 30 px blur). Both become opaque under Reduce transparency.
**Why:** At 62% the text underneath competed with list items in the command palette. Near-opaque panels match macOS menus and stay legible even where blur isn't rendered.

### D-032 · 2026-09-23 · No `Spawner` port; tests use the fake binary
**Decision:** The supervisor spawns processes directly with `tokio::process`. Tests point it at `tools/fake-cloudflared` (a real binary), and the supervisor integration tests live in that package so Cargo always builds the fake first (`CARGO_BIN_EXE_fake-cloudflared`).
**Why:** A real child process exercises signals, process groups, pipes and HTTP polling, which a mocked spawner wouldn't. One fewer abstraction.

### D-033 · 2026-09-23 · Merging to `main` needs the maintainer; milestones stack
**Decision:** Unattended sessions don't merge PRs (the environment's permission policy blocks merge-without-review, rightly). When a milestone is done, its PR is marked ready and the next milestone continues on a branch stacked on it, with a draft PR targeting the previous milestone branch. After the maintainer merges, the next PR is retargeted to `main`.

### D-034 · 2026-09-23 · A Quick Share is "live" only when a connection is registered
**Decision:** Show the URL as ready only when `/quicktunnel` has a hostname **and** `/ready` reports ≥ 1 connection.
**Why:** Measured with cloudflared 2026.9.1: `/quicktunnel` returns the hostname about 2 s before the first edge connection registers (`readyConnections: 0`); opening the URL in that window fails.

### D-035 · 2026-09-23 · E2E harness is compiled in only with `--features e2e`
**Decision:** The embedded WebDriver (`tauri-plugin-wdio-webdriver`), `tauri-plugin-wdio`, its JS bridge, its capability and `withGlobalTauri` are enabled only in E2E builds (`pnpm e2e:build`: cargo feature `e2e`, a `--config` overlay, `VITE_E2E=1`). Release builds contain none of it (checked: no wdio code in `dist/`). E2E builds refuse to start unless `TEITUNNEL_CLOUDFLARED` points at a binary, and `TEITUNNEL_DATA_DIR` isolates their data.
**Why:** An embedded WebDriver can drive the entire UI. The first E2E attempt, before the guard, ran the real cloudflared and opened a real Quick Share; it was stopped immediately.

### D-036 · 2026-09-23 · Exit animations are skipped while the page is hidden
**Decision:** List exit animations only run when `document.visibilityState` is `visible`.
**Why:** WebKit runs no animation frames for a hidden window, so a share stopped from the menu bar would linger in the list until the window reappeared and then animate out. Verified by the E2E run (0 frames while hidden).

### D-037 · 2026-09-23 · Quick Shares go "Live" 6 s after the hostname appears, without DNS queries
**Decision:** After `/quicktunnel` reports a hostname and a connection is registered, wait until 6 s have passed since the hostname first appeared before marking the share live (the URL is shown meanwhile, Open disabled). Teitunnel never queries DNS for the new name itself.
**Why:** Measured: new names aren't resolvable for ~2–3 s, and NXDOMAIN is cached for up to 30 minutes (SOA minimum 1800). A DoH readiness check was tried and rejected, because querying 1.1.1.1 too early caches the NXDOMAIN there, breaking the URL for everyone using that resolver. The nightly real test confirms the approach (public fetch succeeds on the first try).

### D-038 · 2026-09-23 · Permission probing by PATCHing a nil id
**Decision:** Capability checks never write. Reads use one-item lists; write permission is inferred from `PATCH …/<all-zero id>`: 404 means authorized (no such object), 403 / code 10000 means not authorized.
**Why:** API tokens can't read their own permissions without an extra "API Tokens Read" grant. A write probe on a nil id is side-effect free by construction; tests assert no POST/DELETE is ever sent.

### D-039 · 2026-09-23 · The active account is UI state
**Decision:** Which account Domains/Routes show is remembered in the persisted UI store, not in the backend; the engine works with all accounts.
**Why:** ARCHITECTURE §6 (all accounts active in the engine). Keeps commands stateless (`domains_list(accountId)`).

### D-040 · 2026-09-23 · Route checks bypass DNS entirely
**Decision:** The verifier confirms the DNS record through the Cloudflare API, then sends its HTTPS probe directly to a Cloudflare edge address (resolved from `api.cloudflare.com`) with the route's hostname as SNI/Host. It never resolves the route's hostname.
**Why:** Same failure mode as D-037: resolving a brand-new name too early caches NXDOMAIN in the local and upstream resolvers for up to the zone's SOA minimum, breaking the URL the user is about to open. Cloudflare's edge serves any proxied hostname on any of its anycast addresses ("Addressing Agility"), so the probe exercises the real edge → tunnel → origin path without touching DNS.


### D-041 · 2026-09-23 · The machine tunnel is never adopted by name
**Decision:** A new machine tunnel is named after the host name; if the account already has a tunnel with that name, the new one becomes "name 2", "name 3"… Teitunnel only reuses the tunnel recorded in `tunnels_local`.
**Why:** Two Macs can share a host name. Adopting by name would make both run connectors for one tunnel, and Cloudflare would load-balance one route across two machines. Import of foreign tunnels is an explicit M4 flow.

### D-042 · 2026-09-23 · Route ids are derived from hostname and path
**Decision:** A route's id (written into its DNS comment, `teitunnel:route=<id>`) is the first 6 bytes of SHA-256(hostname ∖0 path), hex.
**Why:** Preview and apply are separate IPC calls that re-plan; a derived id makes both plans identical without storing state in between, and the ownership comment stays recoverable from the route itself.

### D-043 · 2026-09-23 · E2E runs against a fake Cloudflare with in-memory secrets
**Decision:** `tools/fake-cloudflare` implements the API endpoints the engine uses and answers route probes as the edge. Builds with the `e2e` feature read `TEITUNNEL_API_BASE` / `TEITUNNEL_EDGE` and use `MemoryStore` for secrets; release builds can't do either.
**Why:** End-to-end tests must never touch the maintainer's Cloudflare account or login keychain, and must be deterministic in CI. The real API is exercised by the nightly job once a test token exists.

### D-044 · 2026-09-23 · Importing an existing setup moves its routes onto this Mac's tunnel
**Decision:** Routes found in `config.yml` files are imported as routes of this Mac's remotely-managed tunnel (`Intent::ImportRoutes`), one combined plan. DNS records that point at the old tunnel aren't Teitunnel's, so repointing them needs confirmation. The files and the old tunnel are left untouched. Per-route `originRequest` settings block a route's import for now; global ones are reported. YAML is read with `serde-saphyr` (maintained, deserialize-only; `serde_yaml` is deprecated and `serde_yml` has a RustSec unsoundness advisory).
**Why:** Keeps one model (routes on one machine tunnel, plan → apply, ownership) instead of a second locally-managed mode with YAML rewriting, and leaves the user a working fallback until they delete the old setup themselves.

### D-045 · 2026-09-23 · Always-on connectors are launchd agents the app installs, never supervises
**Decision:** Always-on runs `cloudflared tunnel run --token-file` as a per-user launch agent (`com.teispace.teitunnel.connector.<id>`, KeepAlive, RunAtLoad) with its JSON log redirected to `<app_data>/logs/connectors/<id>.log`. The app writes the plist and runs `/bin/launchctl bootstrap|bootout|print gui/<uid>` through typed builders; health comes from the connector's `/ready`. Switching modes starts the new connector on a fresh metrics port and stops the old one only after the new one has an edge connection. E2E builds substitute `ProcessServices` (child processes) so tests never install real agents.
**Why:** launchd gives restart-on-crash and start-at-login without a privileged helper; keeping the app an observer means quitting it can't take routes down. A fresh port avoids clashing with the running connector during the switch.


### D-046 · 2026-09-23 · Live traffic is polled with a cursor; sampling speeds up while polled
**Decision:** The UI reads live traffic with `tunnels_traffic(tunnel, since)` once a second and appends only the samples newer than `since`. Each read renews a 5 s lease that samples that connector at 1 s; otherwise it is sampled every 10 s. The last 3,600 samples stay in memory per tunnel; finished minutes are persisted to `metrics_rollup` (7-day retention, pruned on write, upserted so restarts mid-minute add up) and served bucketed (5 min for a day, 30 min for a week) by `tunnels_traffic_history`. Series cross IPC as columns (`TrafficSeries`), the shape uPlot draws. Charts are uPlot on canvas (`TimeSeriesChart`) with colours resolved from the tokens at paint time; the SVG `Sparkline` stays for tiny inline use.
**Why:** A pull with a cursor needs no subscription bookkeeping: nothing leaks when a view unmounts, the webview reloads or the window hides (TanStack pauses polling), and a missed poll loses nothing. The lease keeps the scrape rate tied to someone actually looking. Columns roughly halve the JSON and need no reshaping before drawing.

### D-047 · 2026-09-23 · Activity entries carry a structured record next to their text
**Decision:** Each applied plan stores an `ActivityRecord` (JSON in `activity.record`, migration 6): its kind, every hostname involved, each step's `StepView` with its final `StepState` (taken from the same progress events the UI sees), and a before/after list (`Delta`) derived purely from the plan (ingress `previous` vs new, keyed by hostname + path; DNS records' previous values). The plain `detail` lines stay for search and for entries written before the record existed; a record a newer version wrote that this one can't read falls back to them. Steps a rollback reverses implicitly (a new tunnel's config goes with the tunnel) are reported `Undone` once the rollback succeeds. The Activity view filters by kind or problems and by domain (the account's zones, matched as apex or subdomain; a tunnel filter is moot with one machine tunnel per account), shows failed changes as "Attempted Changes" in neutral colours, and offers "Check" only for hostnames that are routed now.
**Why:** Diffs, per-step commands and filters need structure; re-deriving it from sentences would be fragile. Recording from the executor's own events means the log can't disagree with what the user watched happen.

### D-048 · 2026-09-23 · Doctor notifications: one monitor fed by every run; ignores live in settings
**Decision:** `core::doctor_monitor::DoctorMonitor` (pure, like `health`) records every Doctor run, whether the window asked (`doctor_run`) or the background loop did, and returns at most one coalesced notice per run for errors that are new since the previous run. It never notifies for ignored issues, connector outages (`health` covers those) or `account.unreachable` (usually a network blip). The background loop checks every minute and runs only when nothing ran in the last 5 minutes, so while the window is visible its own 5-minute polling is the only run and Cloudflare isn't queried twice; when the window is hidden (TanStack pauses polling) the loop takes over. Ignored issue ids moved from the window's local storage to `settings.ignoredIssues` (changed atomically with `doctor_set_ignored`); the window moves any old local ignores there once. Settings ▸ Notifications ▸ Problems turns the notices off.
**Why:** Problems matter most when the window is closed, and the backend can't honour ignores it can't see. A shared monitor keeps "new since last time" consistent no matter who ran the Doctor.

### D-049 · 2026-09-23 · A cloudflared update moves running connectors over without a gap
**Decision:** The managed binary keeps one path (swapped by rename), so launch agents never point at a missing file, but running connectors keep the old version until restarted. After an install that changes the version or path, the app restarts this Mac's connectors one account at a time (`MachineTunnels::restart_on_current_binary`). An Always-on connector is bridged: a temporary app connector starts and must connect, then the agent is reinstalled on a fresh port and must be ready, and only then does the bridge stop. If the reinstall fails the bridge keeps serving until the next launch reinstalls the agent. A Session connector restarts in place (a few seconds, under the 20 s before "down" notifies). Failures notify and leave whatever was serving running.
**Why:** Updating cloudflared shouldn't take routes down, and a connector that silently stays on an old version defeats the update. Replicas of the same tunnel can run side by side, which makes the bridge safe.

### D-050 · 2026-09-23 · Connector logs: bounded files, tail reads, per-route filtering in the backend
**Decision:** Always-on connector logs (a launchd `StandardOutPath` the agent appends to) are cut back once they pass 8 MiB by copying to `<name>.1` and truncating in place (renaming would leave launchd writing to the renamed file), checked on every sample. Reads take only the tail, backwards in 64 KiB blocks, never more than 2 MiB. `routes_logs` filters a route's lines in the backend by the matched rule's index and service from the ingress Teitunnel applied (`connector_logs::RouteFilter`); level and text filters stay in the window, where they respond as you type over at most 1,000 lines.
**Why:** An unrotated file grows for as long as the agent runs, and reading it whole on every refresh scaled with its age. Filtering by route needs the applied ingress, which only the backend has; level/text filtering in the window costs nothing at this size.

### D-051 · 2026-09-23 · One neutral service definition; exit criteria are measured, not asserted
**Decision:** `cloudflared::service::ServiceSpec` (label, program, args, log file; tokens only from a file) replaces the launchd-specific `LaunchAgent`; `launchd`, `systemd` and `task_scheduler` each render it and build their own typed commands, and `core::service::{Launchd, Systemd, TaskScheduler}` implement `ServiceManager` over it. Only launchd is selected at runtime until M7/M8. Performance exit criteria get a dev-only bench (`src/dev/stress.tsx`) and a script (`pnpm --filter @teitunnel/desktop perf [seconds]`) that drives it in WebKit and reports frame percentiles and DOM size.
**Result (2026-09-23, WebKit, 30 s):** log at ~2,000 lines/s plus a 3,600-point chart at 20 Hz together: 60 fps, p50 17.0 ms, p99 19.0 ms, max 20.0 ms, no frame over 50 ms; DOM constant at 186 nodes.
**Why:** The platforms differ only in how the same thing is written down and started, so the spec shouldn't be launchd's. Claims like "60 fps at 2,000 lines/s" should be re-checkable by anyone, any time.

### D-052 · 2026-09-23 · Accessibility: native colours by default, WCAG AA under Increase Contrast, audited in CI-able script
**Decision:** The default appearance keeps macOS's label and accent colours, whose secondary/tertiary text and white-on-accent sit below 4.5:1 by Apple's design. With Increase Contrast on, the app meets WCAG 2.2 AA contrast everywhere: stronger text tiers, an `--accent-fill` token (accent mixed 78% with black) for every fill that carries white text, and Apple's accessible status colours, nudged to keep 4.5:1 on grey control surfaces. `pnpm --filter @teitunnel/desktop a11y` runs axe-core on every screen in WebKit in light/dark × default/increased contrast: all rules except colour contrast in the default appearance, all rules under Increase Contrast. Result on 2026-09-23: no violations. A manual VoiceOver pass still needs an unlocked screen.
**Why:** Matching the native look is a product goal, and macOS provides Increase Contrast precisely for people who need more contrast. Honouring it fully, and checking it automatically, serves them without making the default look non-native.

### D-053 · 2026-09-23 · Docs site: Astro Starlight in `apps/site`, deployed to GitHub Pages when the maintainer opts in
**Decision:** The user docs are an Astro Starlight site in `apps/site` (Astro 7.3, Starlight 0.42; `passthroughImageService`, so no `sharp` or native install script), served at `https://teispace.github.io/teitunnel/`. Sections: Getting started, Concepts, Guides, Reference (the Doctor catalogue mirrors `core::doctor` check ids). Screenshots are WebP made with `shoot` from the dev mocks, which use neutral names only. `.github/workflows/docs.yml` builds it on every change; deploying needs Pages enabled and the repository variable `DEPLOY_DOCS=true`, both maintainer settings. The Help menu's documentation link stays on the README until the site is live.
**Why:** Starlight gives search, navigation, dark mode and accessible markup with no custom code, and lives in the same workspace and lockfile as the app. Keeping deployment opt-in respects that Pages and repository variables are the maintainer's call.

### D-054 · 2026-09-23 · Windows and Linux: services selected at runtime, no console windows, clear credential-store errors
**Decision:** The app picks the service manager per platform at startup: launchd on macOS, `systemd --user` on Linux when a user session exists (`XDG_RUNTIME_DIR`), Task Scheduler on Windows. `ServiceManager::captures_output` tells the machine whether the manager saves the connector's output (launchd, systemd: the app bounds and tails `<tunnel>.log`) or not (Task Scheduler: the connector runs with `--log-directory <logs>/<tunnel>` and rotates its own `cloudflared.log`, 1 MB × 5, per cloudflared's `logger/configuration.go`). Every helper program starts with `CREATE_NO_WINDOW` on Windows. A missing credential store (Linux without a Secret Service) is `SecretError::Unavailable`, whose message says what to install. Code that needs a real Windows or Linux desktop to verify is listed as such in the M7/M8 plans.
**Why:** The platform differences are small and belong in the adapters, not in the machine or the UI; CI can compile and test all of it, and what it can't is recorded rather than assumed to work.

### D-055 · 2026-09-23 · Export renders from the observed state; Terraform adopts via import blocks
**Decision:** `core::export::render` turns what the engine observed (this Mac's tunnel, its ingress, and the proxied CNAMEs pointing at it) into `config.yml`, Docker Compose or Terraform. Terraform targets the Cloudflare provider v5 (`cloudflare_zero_trust_tunnel_cloudflared`, `…_config`, `cloudflare_dns_record`, verified against the provider docs at v5.25.0) and emits `import` blocks (`<account>/<tunnel>`, `<zone>/<record>`), with `originRequest` keys converted to the provider's snake_case and each record's comment, TTL and proxied flag kept, so a first `terraform plan` shows no changes. Strings go through JSON quoting (valid YAML and HCL) and HCL template sequences are escaped. No export contains a secret; the Compose file reads the run token from `${TUNNEL_TOKEN}`.
**Why:** Adopting existing resources is what makes an export useful rather than a template to recreate from; rendering from observed state means it reflects what's really in Cloudflare, including edits made elsewhere.

### D-056 · 2026-09-23 · A CLI over the same core, sharing the app's data, never running connectors
**Decision:** `apps/cli` builds `teitunnel-cli` (clap 4.6) over `teitunnel-core`. It opens the app's data folder (`dirs::data_dir()/com.teispace.teitunnel`, or `TEITUNNEL_DATA_DIR`) and keychain, and changes routes through the same `Change → intent → preview → apply → verify` path as the UI, printing the plan and asking (`--yes` to skip, `--replace` for records Teitunnel didn't create). It never starts or stops connectors: `ProbedConnectors` reports each tunnel's state from its remembered metrics port and refuses start/stop, so a new route is served by the app or an Always-on service. The binary isn't called `teitunnel` because on case-insensitive disks it would be the same file as the app's `Teitunnel` in the shared target directory.
**Why:** One engine means the CLI can't diverge from the app's safety rules. A short-lived process owning connectors would take routes down when it exits.

### D-057 · 2026-09-23 · Logins through Access apps Teitunnel owns, up before a route and down after it
**Decision:** A route's "Require a login" is a `self_hosted` Access application per route (domain = hostname plus the path when it's a plain prefix; regex paths are refused), named `Teitunnel · <domain>`, with one inline allow policy of `email`/`email_domain` rules, hidden from the App Launcher. Ownership is a local index (migration 7), so Teitunnel updates or deletes only applications it created; a foreign application on the domain is left alone (the plan errors unless it already allows exactly the requested people). The planner creates or updates the application before config and DNS go live, and deletes it after the route is gone (before the irreversible tunnel delete); every step is undoable (a deleted application is recreated from its observed definition). One-time PIN is added only when the account has no login method; a missing Zero Trust organization is a plan error. Access is observed only when a change needs it (`AccessNeed`), and a token without Access permissions still manages plain routes (owned-app reads degrade on 401/403). Verify treats a redirect to the Access login as "works, protected". On an edit, no rule removes Teitunnel's login; on an add, it leaves any login alone (Doctor fixes re-add routes that way).
Teitunnel's own logins left behind by an outside route removal are a Doctor issue (`access.orphan`, fixed by a planned `RemoveLogin`). The token template doesn't ask for Access permissions (least privilege; most routes don't use logins); asking for a login with a token that lacks them fails with an error naming the two permissions.
**Why:** Protection must never lag behind exposure, and nothing Teitunnel didn't create is changed. Reading Access only when needed keeps tokens without that permission working.

### D-058 · 2026-09-23 · Connectors per machine and remote logs; no one-click replicas
**Decision:** The Tunnels view groups a tunnel's edge connections by connector (the tunnel list's `client_id`, no extra API call) and marks this Mac's using the `connectorId` from its connector's `/ready`. The Doctor warns (`tunnel.other_connectors`) when another machine runs this Mac's tunnel, unless a local twin explains it. Any connector's logs can be followed through Cloudflare's management relay: `cf_api::LogStream` speaks the `start_streaming`/`logs` protocol over `tokio-tungstenite` (rustls, OS roots); `core::remote_logs` keeps one session per connector with a 2,000-line ring, streams level info and above (request logs at debug would flood a busy tunnel), reconnects after transient drops with a fresh token, ends with a reason on the session limit or missing permission, and stops 30 s after the UI last read it. The management token never leaves Rust and is redacted from error texts. "Run the same tunnel on several machines" is not offered as a button.
**Why:** Teitunnel's routes point at this Mac's localhost, so a replica would receive a share of requests for services it doesn't run: intermittent 502s that look random. Seeing every machine and its logs solves the real problem (a forgotten connector somewhere) without adding that footgun; running elsewhere on purpose is what Export is for.

### D-059 · 2026-09-23 · `teitunnel-cli share`: a connector scoped to the command
**Decision:** `share` is the CLI's one exception to D-056: it runs a Quick Share through the same `QuickShares` as the app, for exactly the command's lifetime (Ctrl-C, SIGTERM, SIGHUP or `--for` stop it). Its connectors are recorded in a per-process registry (`<data>/run-cli/<pid>-<start time>/`); each run first reaps the registries of CLIs that are gone (owner pid and start time no longer match), so a SIGKILLed CLI can't leave a public URL behind. Metrics ports are allocated from a per-process offset (`PortAllocator::spread`) so the app and CLIs starting at once don't hand cloudflared the same port. The URL is the only stdout line; progress goes to stderr. It works without the app set up (in-memory history then). `doctor` reuses `doctor::run` (now generic over `Connectors`) with the probed connectors and applies only `doctor::safe_change` fixes.
**Why:** A share started in a terminal belongs to that terminal; nothing about it outlives the command. Reaping by owner identity is what makes that true even when the terminal is killed.
**Superseded in part by D-102:** when the app is running, `share` hands the share to the app and exits; `--here` keeps this behaviour.


### D-060 · 2026-09-23 · Private networks: ranges on this Mac's tunnel in the default virtual network; client commands for non-web routes
**Decision:** "Share a private network" routes a CIDR range (a bare address is a `/32`/`/128`; host bits are cleared; ranges broader than /8 or /16 and loopback, link-local and multicast are refused) to this Mac's tunnel through the engine (`Intent::AddNetwork`/`RemoveNetwork`, steps `CreateNetworkRoute`/`DeleteNetworkRoute` with undo), creating the tunnel if needed; the connector is started after. Routes go in the account's default virtual network only; other virtual networks are read (so their routes don't count as conflicts) but not offered. A route belongs to this Mac when it points at this Mac's tunnel, whoever created it (comment `Added by Teitunnel` only labels it); removing a network or the tunnel deletes exactly those, never another tunnel's. The same range on another tunnel is a plan error; overlapping ranges and public ranges are warnings, and a public range needs confirmation (`--replace` in the CLI). Private networks are read only when a change or view needs them (`ObserveNeed::networks`, `Want::IfAllowed` for the overview, tunnel removal and the Doctor, so tokens without the permission keep working). The Doctor reads the WARP device settings only when this Mac shares a network and warns when the Gateway proxy is off (`network.proxy_off`) or the default profile's Split Tunnels exclude the range (Cloudflare's default excludes RFC 1918) or don't include it (`network.excluded`, `network.not_included`); it can't fix them (they're account-wide Zero Trust settings). SSH, RDP, SMB and TCP routes show the `cloudflared access` command visitors run (plus an `~/.ssh/config` entry for SSH) instead of a URL, and aren't checked with an HTTPS probe after applying.
**Why:** One virtual network covers the home-lab and small-team case without asking users to understand namespaces. Split Tunnels are the most common reason a correctly routed network "doesn't work", so surfacing them where the network is listed saves the debugging. A browser probe of an SSH route would always report a failure.

### D-061 · 2026-09-23 · i18n: an in-house typed catalog, the system language, English text from Rust for now
**Decision:** The UI's text lives in JSON catalogs (now `locales/<language>.json` at the repository root, see D-062); English is the source, and `MessageKey` is derived from it, so an unknown key is a type error. `t(key, vars)` (`src/lib/i18n.ts`, no dependency) fills `{name}` placeholders, formats numbers for the language and picks plural forms with `Intl.PluralRules` (`_one`/`_other`, plus `_zero`/`_two`/`_few`/`_many` where a language has them). The language follows the system's (`navigator.languages`, which on macOS includes the per-app language), falling back per message to English; catalogs load before the first render, so module-level label maps hold keys, not text. Lists use `Intl.ListFormat`. A test rejects translations with keys English doesn't have or with different placeholders; `pnpm --filter @teitunnel/desktop i18n:missing <language>` lists what's left to translate. Text produced in Rust was English in this phase; D-062 moved it to the same catalogs.
**Why:** A few hundred strings don't need a framework; a typed 100-line module keeps the bundle budget and catches typos at compile time. JSON is what translation platforms (Weblate, Crowdin) read. Following the system language is what Mac apps do.

### D-062 · 2026-09-23 · Text from Rust as message keys, checked at compile time
**Decision:** Rust never sends the user a sentence. User-facing text is a `teitunnel_core::text::Text { key, args }`, serialized over IPC as `{ key, args }` and translated by the UI with the same `t()` as its own messages (`translate()`; `IpcError` translates its message and hint once, and keeps `key` for logic such as recognising a cancelled sign-in). There is one catalog per language, `locales/<language>.json` at the repository root: the UI's messages at the top level, the Rust core's under `core`. The core's `build.rs` generates a typed constructor per `core` message (`msg::doctor::dns_missing::title(hostname)`; plurals take `count: u64` first), so a misspelled key or missing argument doesn't compile; it also embeds every catalog. Errors implement `UserText` (`text()`), and their `Display` renders the catalog in English, so logs and bug reports stay English and the English isn't written twice. Covered: every core error type (and cf-api/cloudflared errors via the core), `AppError`, plan steps and summaries, apply failures and leftovers, the activity record (new optional `summary`, `error`, `leftovers`, `connector_error`; `Text` also deserializes from a plain string, so stored history loads without a migration), Doctor issues (title, detail, evidence, fix labels, and a display `label` separate from the stable `subject`), verify results, Quick Share and remote-log states, import problems, the OAuth callback page (HTML-escaped), and the native menu bar (including the predefined macOS items, via `*_with_text`), tray and notifications, rendered in Rust in the system language (`sys-locale`; plural rules from `intl_pluralrules`, the Fluent project's CLDR data). Technical values (DNS record contents, services, log lines, Cloudflare's own error messages) pass through as `core.raw` arguments, untranslated. Route health in the tray is an enum (`RouteHealth`), not compared text. Two things stay English on purpose: `teitunnel-cli` (a developer tool whose output scripts parse; its `--json` renders messages to English strings), and markers stored in Cloudflare (the `Added by Teitunnel` route comment identifies ownership).
**Why:** Translating on the side that knows the language keeps Rust free of UI state, and compile-time keys make the catalog impossible to drift from the code. One file per language is what a translator (or Weblate) wants.

### D-063 · 2026-09-23 · Windows and Linux: native title bars, solid surfaces, commands in the window, platform wording
**Decision:** On Windows and Linux the main window is opaque with the system's own title bar and caption buttons (snap layouts on Windows come with it): `tauri.windows.conf.json` and `tauri.linux.conf.json` replace the macOS window (transparent, overlay title bar, vibrancy). Surfaces are solid, from `styles/platform-other.css`: Windows 11's Mica-like neutrals, Segoe UI Variable and its default accent; GNOME's Adwaita tones and Cantarell. The navigation selection is a neutral fill (Windows adds its accent pill), and selections don't grey out when the window is inactive, since both are macOS conventions. There's no menu bar there: `menu::build` runs only on macOS, the webview handles shortcuts (Ctrl instead of ⌘, matched on the physical key; `app/shortcuts.ts`), and the command palette carries the Help links and Quit (`app_open_help`, `app_request_quit`, which asks first like ⌘Q). The tray opens the app on left click on Windows (menu on right click); closing the window quits when the tray icon is hidden, since nothing else would bring it back. Messages that name macOS things (this Mac, Finder, keychain, System Settings, the menu bar, ⌘, Homebrew) have `key@windows` and `key@linux` wordings (this PC / this computer, File Explorer, Credential Manager / keyring, notification area / system tray, Ctrl, winget / the package manager), picked by both the UI and the core; a test fails when a new message has macOS words without them. Real Mica is left out for now: it needs a transparent window, which shows the desktop through it on Windows 10, and it can't be verified without a Windows machine. The token file keeps the per-user profile's ACL (only the user, SYSTEM and Administrators can read `AppData`); an explicit ACL would need Win32 FFI (`unsafe`), which the workspace forbids.
**Why:** Each platform's own chrome and wording is what makes the app feel native there, and falling back to macOS materials on a platform without them produces a see-through window.

### D-064 · 2026-09-23 · A missing Access permission is fixed in place, not reported
**Decision:** When a route needs a login and the account's credential can't manage Access, the route sheet shows `AccessFix` instead of an error: ticking **Require a login** with `accessEdit = no`, or a plan refused with `core.error.observe.accessPermission`, both lead there. For API tokens it walks through editing the existing token in the dashboard (`TokenPage::Edit` opens the token list; Cloudflare keeps the token's value when its permissions change, so nothing is pasted) and re-checks whenever the window regains focus or on **Check Again**. When a re-check finds the permission, the form continues, and a refused review runs again. Only explicit re-checks report readiness, so a refusal that persists can't loop. Credentials that can't gain the permission (cloudflared's cert.pem, OAuth until its Access scopes are registered), or users who prefer a new token, get **Create Token** (`TokenPage::CreateWithLogins`: the usual template plus `access` and `access_acct` edit, keys from Cloudflare's documented table) and a paste field that replaces the account's credential through `accounts_add_token`. The default connect template stayed without Access here (least privilege, D-057); D-065 changed that. `accessEdit` now needs both Access permissions: the apps write probe and a read of the login methods (`/access/identity_providers`; its update endpoint is PUT-only, so a PATCH probe would read 405 as allowed).
**Why:** A permission error with nowhere to go makes users disconnect and start over. Editing the token they already have, and noticing when they come back, turns it into one trip to the browser, without asking every user for a broader token up front.

### D-065 · 2026-09-23 · The token template asks for Access up front
**Decision:** The pre-filled "Create API token" link includes `access` and `access_acct` (edit) next to Tunnel, DNS, Zone and Account Settings, so a new token can require logins without a second trip to the dashboard. Supersedes the least-privilege default in D-057/D-064. The fix-in-place card (D-064) stays for older or hand-made tokens and for cert.pem/OAuth accounts. Ownership rules don't change: Teitunnel edits only Access applications it created.
**Why:** The maintainer chose ease over the narrower grant: logins are a first-class route option, and a permission error on first use is the worse experience. The risk (a leaked token could change other Access apps) is the same class as DNS · Edit on every zone, which the token already has.

### D-066 · 2026-09-23 · No dead ends: every core error is fixed in place, linked, or explained
**Decision:** `PermissionFix` (generalised from D-064's Access card) lists what a feature needs that the credential lacks (`PermissionNeed`: tunnels, zones, DNS per domain or on any domain, Access), in the dashboard's own names, with why, and offers "edit the existing token" (nothing to paste) or "paste a new one". It re-checks on window focus and calls back so the feature resumes. It appears in the route sheet (a gap found by the capability check disables Review; a refusal, whether Access or Cloudflare's generic permission error, shows the change's needs), the Doctor's `auth.missing_scope`, private networks, connector logs, and under each account's and domain's permission list (whose per-row hints it replaces). `ZeroTrustFix` opens Zero Trust onboarding and retries on return. Every `core.error.*` message is classified in `lib/error-help.ts` as `fix`, `link` (a button to the Cloudflare page that resolves it: add a domain, Access applications), `input` or `retry`; a test fails when a new error isn't classified.
**Why:** A user who hits a wall has to know where to go and has to be brought back to where they were; making the classification exhaustive keeps new errors from regressing to plain text.

### D-067 · 2026-09-23 · Several tunnels per machine, one planner per tunnel
**Decision:** A machine can have more tunnels besides its default (machine) tunnel (M10-02, Cloudify parity: environments). Local state is one row per tunnel (`local_tunnels`, migration 8, `is_default`; the old per-account row becomes the default), and everything keyed by account that was really about the tunnel is now keyed by tunnel id (applied version and ingress, metrics port, run mode). The engine stays single-tunnel: `Context.tunnel` picks which of this Mac's tunnels a change is about (`None`: the default; an unknown id is `ObserveError::UnknownTunnel`), so planning, rollback and the staleness guard are unchanged. A new tunnel is `Intent::CreateTunnel { name }` (plan → apply, name 1–64 characters and unique in the account, `TunnelNameTaken` otherwise); a created tunnel is recorded as the default, an additional one, or a replacement of the row it recreates (`Slot`). A hostname is routed once per machine: the snapshot carries the routes of the other local tunnels (`elsewhere`, from the local store), and the planner refuses a duplicate (`RoutedElsewhere`). The overview merges every local tunnel (routes carry `tunnel_id`; health comes from the carrying tunnel's connector); verify and route logs find the carrying tunnel themselves; drift is checked on every tunnel; connectors resume, update, quit-to-Always-on and sign-out per tunnel; the Doctor gathers and diagnoses each tunnel separately, deduplicates account-wide findings, and fixes on the issue's tunnel (`Issue.tunnel_id`); logins on another tunnel's routes aren't orphans. Private networks stay on the default tunnel. CLI: `tunnels`, `tunnel create|delete`, `--tunnel` on changes and export.
**Why:** Keeping the planner per tunnel makes the feature additive: every guarantee (plan → apply, ownership, undo) holds per tunnel without a multi-tunnel planner, and one-hostname-per-machine keeps DNS unambiguous.

### D-068 · 2026-09-23 · Share on your domain: temporary routes owned by who started them
**Decision:** A Quick Share can use a hostname on one of the account's active domains (M10-03, Cloudify's `--subdomain`, which trycloudflare can't honour). It's a normal route on this Mac's default tunnel, made and removed through plan → apply (`domain_shares::start/stop`), plus a row in `domain_shares` (migration 9: account, hostname, origin, owner, optional expiry). The row is written before the route is applied (so a crash can't leave an untracked route) and deleted after it's removed; an already-gone route counts as stopped. A share never replaces a DNS record Teitunnel didn't create (a plan needing confirmation is refused). The owner is `app` or a CLI process (`runtime::this_process`: pid and start time, so pid reuse can't keep it alive). The app ends the expired and those of exited CLIs every 30 s, its own from a previous run at launch, and its own on quit (before connectors stop). The Routes view marks them *Temporary*; the Quick Share page lists them next to trycloudflare shares. CLI: `share <origin> --on <hostname> [--for] [--allow] [--account]` for the command's lifetime.
**Why:** A stable, memorable URL for a demo without committing a permanent route, and the same guarantees as routes (reviewable plan machinery, ownership, cleanup) instead of a second mechanism.

### D-069 · 2026-09-23 · Headless servers and containers: the token from the environment, connectors from `up`
**Decision:** `teitunnel-cli` works without the app (M10-04/05, Cloudify's server and Docker modes). Where there's no keychain, the API token comes from `CLOUDFLARE_API_TOKEN` / `TEITUNNEL_API_TOKEN` or their `…_FILE` variants (Docker/Kubernetes secrets): it's verified, kept in a `MemoryStore` for that command, and never written anywhere (a test walks the data directory for it); the accounts it reaches are recorded as usual (no secret in the database). `teitunnel-cli setup` stores a token in the OS keychain where one exists. So the "secrets only in the keychain" rule holds; the only file-based secret stays the tunnel run token of an Always-on service (0600), as before. `teitunnel-cli up` runs every tunnel of the machine in the foreground (Supervisor + `MachineTunnels`, per-process registry so a killed `up` is reaped), leaves Always-on tunnels to their service, and stops on SIGINT/SIGTERM: the entrypoint for containers and any process supervisor, so no cron fallback is shipped (cron would run a shell). `always-on on|off|status` uses `service::for_this_platform`: as root on a systemd host, system units in `/etc/systemd/system` (boot, `multi-user.target`, sandboxed: `NoNewPrivileges`, `ProtectSystem=strict`, `ProtectHome=read-only`, `PrivateTmp`, `PrivateDevices`, kernel protections, only the log directory writable); otherwise the user's manager. `routes --check` exits 1 unless every route is live (health checks). The Docker image (`docker/Dockerfile`) is the CLI built from source plus Cloudflare's own `cloudflared` image (pinned), on distroless `nonroot`, state in `/data` (owned by `nonroot`), `up` by default, `routes --check` as `HEALTHCHECK`; CI builds and smoke-tests it (publishing comes with the release work). The CLI's English wording says "this machine", not "this Mac". API base and edge overrides (`TEITUNNEL_API_BASE`, `TEITUNNEL_EDGE`) exist only in debug builds, so a release build can't be told to send the token elsewhere.
**Why:** The same engine and guarantees on a server as on a desktop, without weakening where secrets live; `up` under the platform's supervisor is simpler and safer than generating cron or init scripts.

### D-070 · 2026-09-23 · A server dashboard and API, small and locked down
**Decision:** `teitunnel-cli serve` runs the machine's tunnels (like `up`) plus a web dashboard and JSON API (M10-06, Cloudify's web interface and API). It isn't the desktop UI in a browser: that would mean re-hosting ~80 Tauri commands; instead a focused dashboard (tunnels, routes with status, add/remove routes and tunnels through a reviewed plan, shares) over three calls: `GET /api/overview`, `POST /api/preview` (plan, nothing changes) and `POST /api/apply` (applies exactly the reviewed plan by fingerprint, 409 if Cloudflare changed), described at `/api/openapi.json`. Messages are rendered to English on the server. Security: loopback by default (`--allow-remote` to bind elsewhere, behind TLS or published through Teitunnel with a login); one password (argon2id, `serve --set-password` or `TEITUNNEL_WEB_PASSWORD`), refused to start without one; sign-in rate-limited per address (5 per 5 min); sessions are random 256-bit tokens in memory in an `HttpOnly; SameSite=Strict` cookie (`Secure` with `--secure-cookies`); cookie-authenticated requests that change anything (and sign-in) need the `X-Teitunnel` header, and no CORS headers are sent, so another site can't drive it; API keys (`ttk_…`, 256-bit, stored as SHA-256, shown once, revocable) for automation via `Authorization: Bearer`; strict CSP (no inline or remote code), `X-Frame-Options: DENY`, `nosniff`, `no-referrer`, `no-store`; the page builds DOM with `textContent` only. Credentials live in `web_credentials` (migration 10), hashes only.
**Why:** Parity with a web-managed tool without turning the desktop app into a web server, and with the attack surface of a small, documented API.

### D-071 · 2026-09-23 · Load balancing across machines, the way Cloudflare documents it for tunnels
**Decision:** A route can be load balanced across every tunnel in the account that routes its hostname (M10-07; Cloudify advertised load balancing but didn't implement it). Per Cloudflare's docs for published applications behind tunnels: one pool whose endpoints are `<tunnel id>.cfargotunnel.com` with the hostname as Host header, a monitor (HTTPS `/`, 2xx after redirects, same Host header), and a load balancer named after the hostname, `proxied`, pool as default and fallback; it takes precedence over the DNS record, which stays as a fallback. Everything is plan → apply: `Intent::BalanceRoute`/`UnbalanceRoute` and steps `CreateLbMonitor`, `CreateLbPool`/`UpdateLbPool` (endpoints as `TunnelRef`s, so a tunnel created in the same plan can join), `CreateLoadBalancer`, and the three deletes, each undoable; rolling back a delete recreates monitor, pool and load balancer in order with the new ids remapped. Teitunnel marks its objects with `teitunnel:lb=<hostname>` in their description, changes only those (a foreign load balancer is `BalancerExists`), and records balanced hostnames locally (migration 11) for the routes view. Adding a route to a balanced hostname joins the pool instead of repointing DNS; removing it leaves the pool, and the last one out removes the load balancing. Reading load balancing is required for balance changes and optional (any failure means "not balanced") for adding or removing routes, so an account without the add-on is never blocked. The token template can't pre-select these permissions (no documented short keys); `PermissionFix` lists them on refusal. Per-endpoint health (follow-up, same day): the route's inspector lists the pool's endpoints with Cloudflare's pool health (`GET …/pools/{id}/health`, parsed leniently: region → origins keyed by address), summarised per endpoint as healthy regions of those checking it plus the first failure reason, polled every 30 s only while shown; nothing is stored.
**Why:** Real failover for a hostname served from several machines, without Teitunnel touching load balancing it didn't create, and without extra cost for anyone who doesn't use it.

### D-072 · 2026-09-23 · Adopting an existing tunnel is local and explicit
**Decision:** "Run on This Mac…" / `teitunnel-cli tunnel adopt` records an existing, remotely configured tunnel of the account as one of this machine's (not the default) and starts its connector with a token fetched from Cloudflare. Nothing changes in Cloudflare, so there's no plan. Locally configured tunnels are refused (their routes live in a file; import them instead). When another machine runs the tunnel, the confirmation says Cloudflare will split requests between both (the reason D-058 kept replicas out of one-click actions), and points to load balancing for serving one hostname from several machines on purpose.
**Why:** Cloudify's `adopt` without the silent traffic-splitting surprise.

### D-073 · 2026-09-23 · One website: a product landing page and Fumadocs docs, statically exported
**Decision:** The docs site moves from Astro Starlight (`apps/site`, D-053) to `apps/web`: Next.js 16 with Fumadocs 16 (docs at `/docs`, sidebar from `meta.json`, static search index from `/api/search`), Tailwind 4, and a landing page at `/` in the style the maintainer asked for (cursor.com): a product-led hero with the real app, three feature stories with screenshots, a capability grid linking into the docs, how it works, platforms, security, FAQ and a closing call to action. No testimonials, logos or numbers that aren't real. Screenshots come from the app itself (`pnpm --filter @teitunnel/desktop shoot` with `?clean`, which hides the developer section, and the new `SHOOT_FILL_LATE`/`SHOOT_THEN` for sheets), in light and dark, shown per the reader's scheme. The site is a static export (`output: "export"`) for **https://teitunnel.teispace.com** (maintainer, 2026-09-23): GitHub Pages with a custom domain (`public/CNAME`), served at the root; the Cloudflare DNS record (`teitunnel` CNAME to `teispace.github.io`) is made when it's deployed, with the release work, and the app's Help link moves from the README to the site then. Its TypeScript is 5.x (Next's type check needs the classic compiler API; the desktop app uses 7). Every page was migrated (asides as `<Callout>`, links without the old base) and reworded for every platform; new pages: Accounts and permissions, Server API; Install covers macOS, Windows, Linux, the CLI and Docker.
**Why:** One place that sells the product and documents all of it, on the stack the maintainer asked for, deployable as static files anywhere.

### D-074 · 2026-09-23 · Distribution: website downloads from GitHub Releases, release-please, SignPath later
**Decision (maintainer + agent):** First public beta is **v0.1.0**, a normal (not pre-release) GitHub Release labelled Beta on the website, because `releases/latest`, which the updater and website use, skips pre-releases. Downloads are offered **only on the website** (teitunnel.teispace.com) for now; files are hosted as GitHub Release assets and linked by exact version from the static site, which the release workflow rebuilds. Homebrew, winget, Scoop and ghcr come later. Formats: macOS universal `.dmg` (Developer ID of the organization Teispace, notarized); Windows NSIS `Setup.exe` per user, x64 and arm64; Linux `.deb`, `.rpm`, AppImage, x64 and arm64 (built on Ubuntu 22.04 for glibc reach); every build native on its own GitHub runner. No Mac App Store, Flatpak or Snap (sandboxes rule out launchd/systemd services and running cloudflared); MSI maybe later for companies. Updates: `tauri-plugin-updater` for every format (`.deb`/`.rpm` via pkexec), minisign key in CI secrets only. Versions: release-please (Conventional Commits → release PR with CHANGELOG → tag → draft release → all builds upload → one job publishes), one version for app, CLI and image. Windows signing: **SignPath Foundation** (Teispace is registered in Nepal, so Microsoft Artifact Signing isn't available); v0.1.0 ships unsigned for Windows, and SignPath is applied for after it is public (their terms require a released project); the workflow re-signs the updater signature after Authenticode signing. Trust: SHA256SUMS, GitHub build provenance, actions pinned by commit, signing secrets in a protected `release` environment.
**Why:** The standard, cheapest path with no servers to run, and nothing half-published: users and the updater only see a release once every file is there. Research: `docs/research/distribution.md`.

### D-075 · 2026-09-23 · App updates: download quietly, install at restart or quit
**Decision:** `tauri-plugin-updater` 2.12 with the endpoint `https://github.com/teispace/teitunnel/releases/latest/download/latest.json` and a minisign public key in `tauri.conf.json` (the keypair was generated on the maintainer's Mac under `~/.tauri/teitunnel/`, outside the repo; the private key and its password go into the `release` environment's secrets and an offline backup). The app checks 15 s after launch and then daily (hourly wake-ups, so a Mac that slept checks soon after), unless **Check for updates automatically** is off (a new setting, default on); a manual check (Settings, or the app menu's Check for Updates…) always runs. A found update is downloaded and its signature verified in the background, then kept in memory; Settings shows the version, progress, "ready" and release notes, and the sidebar shows a quiet "Restart to Update" notice. Installing happens at the end of the normal quit (after the app's connectors and shares stop): on "Restart to Update" it installs and relaunches; on a plain quit it installs by itself for the app bundle, AppImage and NSIS installs, but not `.deb`/`.rpm`, which would ask for a password at quit. Development and E2E builds, and copies whose bundle type is unknown, don't update and say why. Policy lives in `teitunnel_core::updates`, the plugin adapter in `shell::updates`. `createUpdaterArtifacts` is set only for release builds, so local builds don't need the private key.
**Why:** Updates that never interrupt, can't be tampered with, and send nothing about the user (D-019), in the familiar shape of Mac apps (Settings + a restart prompt).

### D-076 · 2026-09-23 · Download pages built from the latest release, detected per visitor
**Decision:** The website reads the latest GitHub release when it's built (`apps/web/lib/release.ts`, with the workflow's token; 404 means "no release yet", other failures fail the build only when `REQUIRE_RELEASE` is set, which the release workflow's dispatch does) and knows each file by its name pattern (the same names the release workflow produces). The main button starts as a link to `/download/` (static HTML, and without JavaScript) and becomes the visitor's file in the browser: macOS gets the universal `.dmg` (Safari can't tell Apple silicon from Intel), Windows the x64 or Arm64 installer (User-Agent Client Hints in Chromium, x64 otherwise, the other one linked), Linux the `.deb` for its architecture with the other formats linked, phones and unknown systems the download page. Choosing a file opens `/download/thanks/?file=<name>` (Slack's pattern), which starts the download only for a file name of the release (no open redirect) and shows install steps for that format, incl. the SmartScreen note while Windows is unsigned. New pages: Privacy and Code signing policy (SignPath's requirements, D-074) and docs "Verify your download". The choice logic is pure (`lib/pick.ts`, `lib/detect.ts`) and tested with `node --test`; `PREVIEW_RELEASE=1` fills in a sample release for local previews (ignored in CI).
**Why:** The right file in one click for most visitors, every file for everyone else, no server to run, and links that can't go stale or point anywhere but the release.

### D-077 · 2026-09-23 · The CLI ships inside the app and goes on the PATH from Settings
**Decision:** Packaged builds bundle `teitunnel-cli` next to the app binary (`bundle.externalBin` in `tauri.cli.conf.json`, merged only for packaged builds via `--config`, so `cargo` builds and CI checks don't need it; `scripts/release/sidecar.mjs` builds it, lipo'd for universal Macs). **Settings ▸ General ▸ Command line** installs it without elevating and without a shell: on macOS a symlink in `/opt/homebrew/bin` or `/usr/local/bin`, whichever is writable (the bundle path is stable across updates), otherwise the exact `sudo ln -s …` to copy; on Windows a copy in `%LOCALAPPDATA%\Microsoft\WindowsApps` (on every user's PATH); for the AppImage a copy in `~/.local/bin`; `.deb`/`.rpm` already put it in `/usr/bin`. Copies carry a marker file and are refreshed at launch after an update. Only Teitunnel's own link or copy is ever replaced or removed; anything else at that path is left alone and shown as such. Logic in `teitunnel_core::cli_install` (tested with temp directories), commands `cli_status`/`cli_install`/`cli_uninstall`.
**Why:** One download gives both the app and the CLI, kept in step, with no privileged helper, no shell and nothing overwritten that isn't ours.

### D-078 · 2026-09-23 · Typed origin settings, every field, nothing else touched
**Decision:** `domain::OriginOptions` types every `originRequest` field the API accepts for remotely managed tunnels (`tunnel_originRequest` in `cloudflare/api-schemas`, 2026-09-23) except `access` (JWT validation at the origin; kept as found): Host header, certificate name, SNI-to-host, no-TLS-verify, CA pool, HTTP/2, no chunked encoding, the four timeouts (whole seconds, as the API takes them), idle connections, no happy eyeballs, SOCKS proxy type. Defaults are omitted when written, and `apply` only sets or removes the known keys, so settings made elsewhere survive. `RouteInput.options`: on add, the settings to write; on edit, `None` keeps the route's current ones and `Some` replaces the known ones (the app's form shows them all, so it always sends them; Undo restores the previous set). Validation per field (host names, absolute CA path, 1 s–24 h, 1–10 000 connections, `socks`) with field `options`. Import merges a `config.yml`'s top-level `originRequest` into each rule, as cloudflared does, reads Go durations (`30s`, `1m30s`), and only refuses routes whose settings include keys outside this set. The Doctor's re-add-DNS fix carries the route's settings. App: Advanced ▸ Origin settings in the route sheet (three groups); CLI: `route add --no-tls-verify --host-header … --connect-timeout …`.
**Why:** HTTPS origins with self-signed certificates and dev servers that check the Host header are common; they now work without the dashboard, and nothing the user set elsewhere is ever lost.

### D-079 · 2026-09-23 · One account picker: the toolbar, not the sidebar
**Decision:** No sidebar account switcher. Each account-scoped view (Routes, Tunnels, Domains, Activity) has an account picker in its toolbar when more than one account is connected, and they share one active account (`ui-store`), so switching in one switches everywhere. Adding and managing accounts stays in Settings ▸ Accounts and the connect sheet.
**Why:** A second picker in the sidebar would repeat the toolbar's on every screen. The toolbar keeps the choice next to the content it filters, as Mail and Finder do with their scope controls.

### D-080 · 2026-09-23 · Trunk-based on `main`; milestone branches retired
**Decision:** At the maintainer's request, the stacked milestone branches (`milestone/m0-…` to `milestone/m6-…`, PRs #1–#7) were fast-forwarded into `main` (a normal push, not a squash, so all Conventional Commits stay for history and release-please) and then deleted, with the PRs closed as merged into `main`. The redundant `legacy/prototype` branch was deleted; the tag `legacy-prototype` keeps the archive. From now on `main` is the trunk: short-lived `<type>/<topic>` branches, draft PRs, Conventional Commit PR titles, squash merges (the repo's only merge method), branches deleted on merge. Supersedes the stacking part of D-033; merging stays the maintainer's call unless they say otherwise.
**Why:** One source of truth that release-please, the website deploy and the release dry run can run from, and no stale branches to confuse contributors.

### D-081 · 2026-09-23 · Website: motion that degrades to static, SEO built at export time
**Decision:** The landing page animates with CSS only, driven by one small observer: an inline script in `<head>` sets `html.tt-motion` before first paint unless Reduce Motion is on (and removes it after 4 s if the observer never starts), `<Motion>` marks `[data-reveal]` elements as they scroll into view, and the hero plays on load. Without JavaScript, with Reduce Motion, or in a crawler, everything is visible and static. The header is transparent over the hero (`transparentMode: "top"`) and links to page sections. The live demos (plan, CLI session, a Quick Share going live, the Doctor fixing a missing record) use the app's and CLI's real wording (`locales/en.json`, `apps/cli`) and come right after the hero; no logo or tech-stack carousel. The download page follows the pattern of VS Code, Tailscale, JetBrains and Warp (researched 2026-09-23 with Claude, Obsidian, Docker, Zed, Postman, 1Password and Brave): system tiles with the visitor's system selected (or the one in the address, `/download/#linux`), one panel with the main file, every format as architecture chips, install steps or a copyable command, and every file in a collapsed list for no-JS readers and crawlers (`lib/platforms.ts`, tested). SEO is generated by the static export: per-page canonical URLs, Open Graph/Twitter cards rendered to PNG per page (`/og/<slug>/image.png`, `next/og`), `sitemap.xml`, `robots.txt`, `llms.txt`/`llms-full.txt`, and JSON-LD (Organization, WebSite, SoftwareApplication, FAQPage on the home page; TechArticle and BreadcrumbList on docs). Docs gained a Use cases section (expose localhost, dev servers, webhooks, Home Assistant, self-hosting, SSH/TCP, Docker Compose), "What is Cloudflare Tunnel?", a comparison page and Troubleshooting (Cloudflare errors), downloads embedded in the docs (`<Downloads os=… />`), and fresh WebP screenshots (`<Screenshot>`). The footer credits Teispace and keeps the trademark notice as small print.
**Why:** The site should feel alive without costing accessibility, performance or indexability, and rank for what people actually search (Cloudflare Tunnel, cloudflared, expose localhost, the errors they hit), with every claim matching the app.

### D-082 · 2026-09-24 · `main` protected by a ruleset; ready for SignPath Foundation
**Decision:** Repository ruleset "main" (default branch): pull request required (no minimum approvals, since the maintainers are the only ones with write access and outside contributors can't merge), required checks Rust on Linux/macOS/Windows, Web, IPC bindings drift and cargo-deny (all in `ci.yml`, which runs on every pull request), no force pushes, no deletion, no bypass. The code signing policy page follows SignPath Foundation's template (attribution line, committers/reviewers and approvers linked to the organization's people and owners, privacy statement), and the words "Code signing policy" appear in the site footer and on the download page, which also says Windows signing by SignPath Foundation is pending.
**Why:** SignPath Foundation's conditions (https://signpath.org/terms, read 2026-09-24) require reviews of outside changes, MFA, and a visible policy; a protected `main` is also simply safer for a project that ships signed releases.

### D-083 · 2026-09-24 · Website and docs changes never cause an app release
**Decision:** release-please skips commits whose files are all under `apps/web/` or `docs/` (`exclude-paths`). A commit that also touches app code still counts. The website deploys from `main` whenever it changes (`docs.yml`), independent of app versions.
**Why:** After 0.1.0, a `feat(web)` commit opened a 0.2.0 release PR with no change to the app, CLI or image; releasing it would have pushed a pointless update to every user and diluted the changelog.

### D-084 · 2026-09-24 · Channels publish the release's own files
**Decision:** Every channel ships the files of a GitHub release, never a rebuild. The Docker image (`image.yml`) copies the release's Linux `teitunnel-cli` into the same distroless image CI builds from source (`docker/Dockerfile`, `CLI=prebuilt`), after `sha256sum --check` and `gh attestation verify` against `release.yml`; it's multi-arch without emulation, tagged `x.y.z`, `x.y`, `latest`, with SBOM and provenance attested to the registry. Homebrew lives in an organization-wide tap, `teispace/homebrew-tap` (`brew install --cask teispace/tap/teitunnel`, `brew install teispace/tap/teitunnel-cli`), so later Teispace apps join the same tap with their own script and workflow; the tap polls the latest release every three hours and needs no secret in this repository, takes checksums only from a provenance-verified `SHA256SUMS.txt`, and tests install/uninstall on macOS and Linux before pushing. The cask links the app's bundled CLI and zaps the app's folders and Always-on agents; the formula depends on Homebrew's `cloudflared` because the standalone CLI never installs one. winget (`Teispace.Teitunnel`, per-user NSIS, ProductCode `Teitunnel`, manifest 1.12.0) is submitted by the release workflow with Komac (pinned by checksum) once the first version is accepted, with a `public_repo` token from a maintainer. The app's NSIS publisher stays as it is: changing it moves the installer's registry key and could break upgrades.
**Why:** One set of signed, attested files means one thing to verify, and a channel can't drift from what the release and the updater ship. Polling from the tap avoids a cross-repository token for a few hours of delay.

### D-085 · 2026-09-24 · No signing claims on the website for now
**Decision:** SignPath Foundation declined the application for lack of popularity. At the maintainer's request the website no longer has the code signing policy page, the "Signed by Teispace and notarized by Apple" note, or any "Windows signing is on its way" note. The Windows install steps still explain SmartScreen's *More info ▸ Run anyway*, and the verify page still shows how to check downloads (checksums, provenance, `spctl`). The macOS builds stay signed and notarized; nothing changed in the release workflow. Supersedes the website part of D-082.
**Why:** Promising Windows signing that isn't coming soon would mislead; the page can be restored from history when reapplying.

### D-086 · 2026-09-24 · apt and dnf repositories served with the website
**Decision:** Teitunnel's Linux repositories live at `https://teitunnel.teispace.com/linux/` (`deb/` with suite `stable`, component `main`, amd64 and arm64; `rpm/` with both architectures; `teitunnel.repo`; `teitunnel.asc`). They're rebuilt in the website's deploy job (`docs.yml`, which the release workflow already runs after publishing) from the two latest releases, so there is no second host, domain or cross-repository token, and a rollback to the previous version stays possible (`apt install teitunnel=<version>`). Each package is checked against its release's `SHA256SUMS.txt` and build provenance before `scripts/release/linux-repo.sh` builds the metadata (`apt-ftparchive`, `createrepo_c`), signs `InRelease`/`Release.gpg` and `repomd.xml`, and signs each RPM (so `gpgcheck` and `repo_gpgcheck` are both on). The script refuses to sign unless the secret key matches the committed public key. The key is RSA 4096 without expiry for compatibility with older rpm (EdDSA needs rpm 4.16+) and so installed machines never need a new key; it lives in the `release` environment. Users add the repository with `signed-by` (apt) or the `.repo` file (dnf, zypper). Verified end to end in Ubuntu 24.04 and Fedora containers against the real 0.1.0 packages.
**Why:** People expect Linux apps to update with the system. Serving the repository from the same Pages deploy keeps it in step with the website and costs nothing extra; keeping only two versions keeps the site small (Pages has a 1 GB limit).

### D-087 · 2026-09-24 · Every release is installed and launched on Windows and Linux before it's published
**Decision:** `release.yml` has a `check` job between build and publish that installs the packages just built on real systems (Windows 11 arm64 and Windows Server x64 runners; Ubuntu 22.04 x64, Ubuntu 24.04 arm64, Fedora in a container) with the system's own tools (silent NSIS install; `apt`/`dnf`, so dependencies must resolve), checks the Apps entry, Start menu shortcut, desktop entry, icon and bundled CLI, launches the app in light and dark with screenshots (virtual display and a window manager on Linux), checks the tray icon (the icon Windows records under `HKCU\\Control Panel\\NotifyIconSettings`, since Windows 11 hides new icons in the overflow; a StatusNotifierWatcher on the session bus on Linux; not on the Windows 11 arm64 runner, whose screen stays on Windows' first-run setup), and uninstalls it, failing if anything is left. Publishing needs it to pass; dry runs and pull requests that touch the release setup run it too. It found that the 0.1.0 `.deb` and `.rpm` didn't depend on the tray library, so the app aborted at launch on a system without it: the packages now depend on `libayatana-appindicator3-1` (deb) and `libayatana-appindicator3.so.1()(64bit)` (rpm, a library dependency every rpm distribution resolves to its own package), and the app looks for the library where the loader would (`LD_LIBRARY_PATH`, an AppImage's own folder, `/etc/ld.so.conf.d`, the usual system folders) and runs without a tray icon instead of aborting when it's missing. The cloudflared install text no longer claims a code-signature check on Windows and Linux, where only the checksums are checked.
**Why:** Nobody on the team uses Windows or Linux daily, and unit and E2E tests run debug builds on machines that have every development library, so they can't catch a package that doesn't start on a clean system.

### D-088 · 2026-09-24 · CI runs what a change needs, behind one required check
**Decision:** `ci.yml` starts with a `changes` job (`dorny/paths-filter`): a pull request whose files are all under `apps/web/`, `docs/`, Markdown, issue templates or `docs.yml` skips the Rust (3 systems), E2E (3 systems), bindings, Docker and cargo-deny jobs; anything else, including files the filter doesn't know, runs everything. The website is built by `docs.yml` and linted by the always-on Web job. A final job named **CI** needs every job and passes only if each passed or was skipped; it's the one required status check on `main` (a skipped matrix job reports under its unexpanded name, so it can't be required by name), which also makes E2E required. Every action in every workflow is pinned to a commit with its version in a comment (Dependabot keeps them current), every job has a timeout, and pull request runs cancel older runs of the same pull request while every commit on `main` runs in full. Release PRs from release-please don't trigger workflows (GitHub ignores events from the workflow token), so a maintainer closes and reopens them to run CI; the workspace crates' versions in `Cargo.lock` are selected as the packages without a `source`, since release-please's TOML parser doesn't match filters on names.
**Why:** A docs fix ran 12 jobs (about 40 runner-minutes) and queued behind real work; the runner queue, not the tests, set the pace. Tags like `@v7` can be moved by whoever controls the action; commits can't.

### D-089 · 2026-09-24 · Docker Hub is the image's main name; ghcr.io stays the source
**Decision:** The image is published as `teispace/teitunnel` on Docker Hub (the `teispace` organization, free plan) and as `ghcr.io/teispace/teitunnel`. `image.yml` pushes to ghcr.io, attests there, and then copies that index to Docker Hub with `docker buildx imagetools create`, so both registries serve the same digest and the provenance covers both. Docs and commands use `teispace/teitunnel`; verification points at ghcr.io. Docker Hub needs a personal access token (organization access tokens need a paid plan) from an owner of the organization, Read & Write only, kept as `DOCKERHUB_TOKEN` in the `release` environment with `DOCKERHUB_USERNAME` as a variable; the overview is `docker/README.md`, pasted by hand (updating it through the API would need a token that can also delete). Apply to Docker-Sponsored Open Source for unlimited pulls once the image is there.
**Why:** Docker Hub is where people search and the default registry, so `docker run teispace/teitunnel` needs no prefix; ghcr.io keeps the image next to the source and its attestations.

### D-090 · 2026-09-24 · The command comes with the app
**Decision:** Installing the app gives you the command where the system's installer can also take it away again: the Windows installer puts it on the PATH (a copy named `teitunnel.exe` in `%LOCALAPPDATA%\Microsoft\WindowsApps`, no administrator; `windows/installer-hooks.nsh`) and the uninstaller removes it; the Linux `.deb`/`.rpm` install `/usr/bin/teitunnel`; the Homebrew cask links it. Where it would mean writing into folders other tools manage (the macOS disk image: Homebrew's `bin` or `/usr/local/bin`; the AppImage: `~/.local/bin`), the Overview offers it once after setup ("Use Teitunnel from the terminal too?" with Install and Not Now, or the `sudo ln -s …` to run when Teitunnel can't write the folder); either answer is remembered (`settings.cliOfferDismissed`), and Settings ▸ General ▸ Command line stays. The app, installer and uninstaller share one marker file next to the copy (`teitunnel.teitunnel`), so a `teitunnel` that isn't Teitunnel's is never replaced or removed. The release's Windows check proves install, version, uninstall and that a foreign file survives both.
**Why:** People who install a CLI-capable app expect the command to work, and a copy the uninstaller doesn't know about was being left behind; asking on macOS matches what apps like VS Code and Tailscale do there.

### D-091 · 2026-09-24 · The command is `teitunnel`
**Decision:** People type `teitunnel <command>` everywhere: clap's name, help, completions, every message and doc, the Docker entrypoint, the Homebrew formula's binary, the CLI archives' binary, the Linux packages (`/usr/bin/teitunnel` next to the app's `/usr/bin/Teitunnel`; `tauri.cli.linux.conf.json`), the Windows PATH copy and the macOS link. Inside the macOS bundle and the Windows install folder the file stays `teitunnel-cli`, because those file systems ignore case and the app itself is `Teitunnel` (`cli_install::BUNDLED_NAME`). The Cargo package and its developer binary stay `teitunnel-cli` (the desktop crate's binary is already `teitunnel` in the workspace), as do the download file names (`teitunnel-cli_<version>_<os>…`) and the Homebrew formula name, which name the package, not the command. The app's own files keep their names. No compatibility alias: nobody has the old name yet (0.1 shipped the day before).
**Why:** A short command matching the product is what people expect and remember; renaming now costs nothing, renaming later would break scripts and servers.


### D-092 · 2026-09-24 · Actions stay busy until their result is on screen
**Decision:** Every action shows that it's working and finishes only when the screen shows its result. Mutations return `refresh(queryClient, …keys)` (`lib/ipc/query-keys.ts`) from `onSuccess`/`onSettled`, so they stay pending until the data they changed has been fetched again. Removing a route keeps its sheet open until the list no longer has it; stopping a share keeps the card, dimmed (`aria-busy`), until it's gone; the Doctor's fix buttons wait for the checks to run again. `Button`/`IconButton` take `pending` (disabled, a spinner in place of the icon, `aria-busy`). Confirmations that do slow work (Disconnect, stopping a cloudflared Teitunnel didn't start, Adopt) use `ConfirmDialog`, which stays open with a busy button and shows a failure inside the dialog instead of closing first. Toolbar Refresh buttons spin only for a refresh you started (`useManualRefetch`), not during background polling. Views and Settings panes show placeholders (`Skeleton`, `SkeletonSection`) instead of nothing while they load. The Overview says it can't reach Cloudflare instead of "Nothing running yet" when routes fail to load. The sidebar has a Settings row at the bottom that opens the Settings window (the only way in on Windows and Linux apart from the tray and the command palette, since there's no menu bar).
**Why:** After a click the old state stayed on screen for seconds (Cloudflare round trips, then a refetch), so it looked like nothing happened; people click again or assume it failed.

### D-093 · 2026-09-24 · Import finds every cloudflared setup; disconnecting leaves nothing behind
**Decision:** Import searches where cloudflared itself looks (`~/.cloudflared`, `~/.cloudflare-warp`, `~/cloudflare-warp`, `/etc/cloudflared`, `/usr/local/etc/cloudflared`), Homebrew's `etc/cloudflared`, and on Windows the service's `%SystemRoot%\System32\config\systemprofile\.cloudflared`; the home folder comes from `std::env::home_dir()` (Windows has no `HOME`, so import, project names and Docker sockets found nothing there before). Besides `config.yml`/`config.yaml` it reads any other `*.yml`/`*.yaml` in those folders that is a config with routes (per-tunnel files), and the `--config` file of every running cloudflared Teitunnel didn't start; each file once, none over 1 MB. Disconnecting an account first stops its temporary shares on its domains (their routes and records go while the credential still works), then stops this machine's connectors and services, and deletes every row keyed by the account (`ACCOUNT_TABLES`, checked against the schema by a test). The dialog now says the routes go offline instead of claiming they keep working.
**Why:** Import silently found nothing on Windows and missed configs not named `config.yml`; shares of a disconnected account stayed in Cloudflare and in Quick Share with a Stop that could only fail.

### D-094 · 2026-09-24 · Tunnel connections come from their own endpoint
**Decision:** From 2026-10-05 Cloudflare's tunnel list and get responses no longer include `connections` ([changelog](https://developers.cloudflare.com/changelog/post/2026-07-09-tunnel-routes-and-connections-api-changes/)). `cf_api::Client::tunnels`/`tunnel` fill `Tunnel.connections` from `GET …/cfd_tunnel/{id}/connections` for tunnels whose status is `healthy` or `degraded` (the others have none; at most 4 requests at a time), so every caller (Tunnels view, machines per tunnel, Doctor, CLI) keeps working with both response shapes. The CIDR-encoded network route endpoints retiring the same day were never used (routes go by `route_id`). **0.2.0 must be released before 2026-10-05**; 0.1.x shows every tunnel without machines from that day until it updates.
**Why:** The field is optional in our model, so nothing would fail loudly; every tunnel would silently look unconnected.

### D-095 · 2026-09-24 · Current Cloudflare API throughout
**Decision:** An audit of every endpoint Teitunnel calls against Cloudflare's [API deprecations](https://developers.cloudflare.com/fundamentals/api/reference/deprecations/) and changelogs (2026-09-24) led to:
- **DNS type changes:** PATCH can't change a record's type since 2026-06-30, so pointing an existing A/AAAA record at a tunnel failed. A type change now goes through `POST /zones/{zone}/dns_records/batch` (delete then create in one transaction, all or nothing, so the name never goes unanswered); same-type updates still PATCH; undo mirrors it. The engine's fake and the E2E fake refuse type-changing PATCHes like Cloudflare.
- **Tunnel connections:** read from `…/cfd_tunnel/{id}/connections` (D-094); the E2E fake now answers in the new shape only.
- **Account-owned API tokens** (Cloudflare's recommendation for servers and CI) verify under `/accounts/{id}/tokens/verify`; the user endpoint rejects them with code 1000, so they were refused as invalid. The user endpoint is tried first, then each reachable account's. They can't use the Zero Trust client (WARP device) endpoints; the Doctor's WARP checks already skip when refused.
- **Access:** applications are written with `destinations` (the replacement for `self_hosted_domains`, retired 2025-11-21) besides `domain`, and with reusable account policies referenced by `{id, precedence}`: Cloudflare's model since the dashboard stopped making application-scoped ones. The policy is named `Teitunnel · <domain> · <name>`; a failed create deletes it again; updates swap in a new policy and delete Teitunnel's old one; deleting an application deletes Teitunnel's policies no other application uses. Applications made by 0.1 (inline policies) are read and updated the same way. All of it lives in `cf_api`, so the engine, its undo and its scenarios are unchanged.
- Already current: network routes by `route_id`, DNS filters (`name=`, `type=`, `content=`, `comment.contains=`), no `proxy-dns`, no service keys, no Argo Tunnel API, no `notification_email`.
Also noted for later: cloudflared drops Intel macOS and 32-bit Windows builds in 2027 (the managed install needs a fallback there); private hostname routes are GA (2026-08-11); tunnel routes can be created in batches (2026-09-02); per-tunnel permissions (2026-05-21).
**Why:** The maintainer asked for everything on Cloudflare's current API; two of these were live failures (replacing an A record, account-owned tokens).

### D-096 · 2026-09-24 · Dev servers that refuse unknown hosts are fixed in place; Quick Shares ignore stray cloudflared config
**Decision:** The verifier recognises dev-server refusals (Vite and everything on it, webpack-dev-server, Angular, Next.js, Rails, Django) from a bounded read (64 KiB, never an event stream) and offers two fixes: send the origin's own Host header (a Quick Share restarts with `--http-host-header`, so it gets a new address; a route or domain share changes only `httpHostHeader` through a plan), or copy the framework's exact config line. New shares of Vite, Nuxt, Angular and webpack dev servers send their own Host by default (share form Advanced ▸ Host header, CLI `--host-header`/`--no-host-header`); Next.js, Rails, Django, SvelteKit and Astro get the config line first because rewriting Host breaks their origin checks (Next.js checks `Origin`, so no Host button). SSE on a Quick Share suggests sharing on your own domain; 413/429/502/1033 are explained inline. Quick Shares always run with `--config` pointing at a `{}` file in the data folder, because a leftover `~/.cloudflared/config.yml`'s `ingress` wins over `--url`. Research: `docs/research/dev-servers-2026-09.md`.
**Why:** A refused first visit is the most common failure when sharing a dev server, and a stray config file silently broke Quick Shares.
### D-097 · 2026-09-24 · An MCP server for AI agents (M12-03)
**Decision:** A new crate `crates/mcp` (official Rust SDK `rmcp` 3.4.1; protocol 2026-07-28 and 2025-11-25) gives agents the app's abilities through 26 tools (sharing, routes via `plan_change` → `apply_plan` by fingerprint, verify, undo, doctor/fixes, logs, export/import, accounts, activity, traffic), resources with subscriptions, and four prompts. It talks to Teitunnel only through a `Backend` trait (`CoreBackend` over core), so the CLI hosts it today (`teitunnel mcp` on stdio, `/mcp` Streamable HTTP on `teitunnel serve` behind API keys and Origin checks) and the app can host it later. Modes per server: `read-only`, `ask` (default; approval through MCP elicitation, else `needsApproval` and a second call with `confirmed: true`; a person's "no" always wins), `full`; foreign DNS records always need confirmation. Secrets are redacted and credential headers masked in every answer; agent changes are recorded in Activity with an `actor`. `teitunnel mcp install <client>` and Settings ▸ AI Tools write the client's config (Claude Code, Claude Desktop, Cursor, VS Code, Codex, Windsurf, Zed, Gemini CLI), merging only Teitunnel's entry, keeping comments and a backup. No built-in LLM (Q4). Facts: [research/mcp-2026-09.md](research/mcp-2026-09.md).
**Why:** Agents bring the model; Teitunnel's value is the safe engine, so agents must go through the same plans, approvals and Activity as people. Traffic tools work through a `TrafficSource` trait so they light up when the inspector (M12-02) lands.

### D-098 · 2026-09-24 · Edge analytics, uptime and alerts (M12-05)
**Decision:** Per-hostname traffic comes from zone `httpRequestsAdaptiveGroups` (the only dataset with a hostname dimension), one aliased GraphQL query for every host in up to 10 zones; plan limits are read from the `settings` node, wide ranges are chunked, parts a plan refuses are dropped and listed. Answers are cached by range (60 s to 30 min) under a per-account budget of 200 queries per 5 minutes (Cloudflare allows 300), served stale when rate-limited or offline. The token asks for Zone Analytics Read and Account Analytics Read (template keys `analytics`, `account_analytics`; PermissionFix `analytics`). Uptime: the machine serving a route checks it through the edge every minute (GET, verifier classification, a login counts as up); two failures open an incident and a success closes it; nothing is recorded without a TCP baseline to the edge (offline), and failures right after sleep don't count; a settings lease makes app, `up` and `serve` exclusive; raw checks kept 2 days, hourly sums 30. Alerts (route down/recovered, 5xx rate with hysteresis, p95 latency, connector down/back) fire once at start and end, 15-minute cooldown per subject, coalesced, recorded in Activity (`ActivityKind::Alert`), silenced by quiet hours. An `AnalyticsSource` trait orders sources by precision (Lens, edge, connector). Research: `docs/research/cloudflare-analytics.md`.
**Why:** Maintainer decision Q3; accurate outages without false incidents from a closed laptop; never spam.

### D-099 · 2026-09-24 · Snapshots on Workers Static Assets (M12-06)
**Decision:** A Snapshot is a Worker with static assets on the user's account: direct upload (session with a hash manifest, only missing files uploaded), then a version deployed atomically; rollback deploys an older version id (last 100 kept by Cloudflare, 10 manifests locally). Addresses via Workers Custom Domains (DNS and certificate made by Cloudflare) or `<script>.<subdomain>.workers.dev`. Sources: a folder (only regular files inside it; dotfiles, `.env*`, keys, `.git`, `node_modules` never uploaded and listed), a build (detected framework, package manager run with discrete args after confirmation), or a bounded same-origin crawl of a local site. Password: PBKDF2-SHA256 hash made on this computer and stored only as a Worker secret, HMAC-signed `__Host-` cookies; `run_worker_first` only when a password or overlay needs it, so plain Snapshots never spend Worker requests. Access logins reuse Teitunnel's Access apps. Intents Publish/Update/Rollback/Delete through plan → apply with undo; foreign DNS records at the hostname are replaced only with confirmation. Migration 13. Research: `docs/research/cloudflare-snapshots.md`.
**Why:** Maintainer decision Q5; Pages adds nothing Teitunnel uses, and one Worker serves the password and the comments overlay.

### D-100 · 2026-09-24 · Lens, the local inspector proxy (M12-02 core)
**Decision:** `crates/lens` is a pure library: taps on loopback forwarding to an origin or a folder, host-based routing on one listener, and an `Acceptor` hook for TLS. It owns its hyper-based upstream pool (HTTP/1.1 keep-alive, h2, TLS through rustls with the platform verifier) for exact timings, safe retries and upgrades. Bodies stream and are never buffered; captures keep up to 1 MiB per body in a 1,000-exchange ring per tap, masked on every read and export unless explicitly revealed (text search runs on the masked view; login forms are never captured). Also: replay/edit/re-sign, exports (cURL, HTTPie, fetch, raw, HAR, JSON, Markdown), webhook verification for eight providers, stubs, gates (password, secret link, basic, bearer, IP, user agents, bypass paths) with HMAC-signed cookies tied to the credentials, HTML injection on navigations only, paused page, header rules, network simulation and faults with injectable randomness, SSE keep-alive after 25 s at event boundaries (Cloudflare's 100 s idle cutoff), WebSocket frames (last 500, bounded deflate). `localhost` origins try 127.0.0.1 then ::1. The workspace uses one TLS provider, AWS-LC (reqwest's), passed explicitly.
**Why:** The inspector is the biggest gap against ngrok, LocalCan and Pinggy, and the foundation for protection, agents' traffic tools and comments.

### D-101 · 2026-09-24 · Local HTTPS domains: a name-constrained CA, user-level trust (M12-07 part)
**Decision:** `crates/localdomains`: Teitunnel's own root CA (ECDSA P-256, 10 years, pathlen 0) with a critical NameConstraints extension permitting only `localhost`, `test`, `local` and loopback/private IP ranges (enforced by Chrome 112+, Firefox, Apple and webpki), so a stolen key can't sign a real site; key in the keychain behind a `CaKeyStore` port. Leaves are issued on demand via SNI, 30 days, renewed 10 days before expiry, cached per CA. Trust is user-level where the OS allows it (macOS login keychain trust settings, Windows `CurrentUser\Root` via `certutil -user`, NSS databases); the Linux system store, `.test` resolver entries and low-port fixes are returned as privileged actions (copy, pkexec with consent). `.test` uses a loopback DNS responder on 53535 (port 53 on Windows, where NRPT has no port). **macOS lets a normal user bind 443 only on the wildcard address, not on 127.0.0.1**, so Lens listens there and drops connections from other machines (`ports::choose_listener`, `is_loopback_peer`). Research: `docs/research/local-domains.md`.
**Why:** Maintainer decision Q7; mkcert's CA isn't constrained; no sudo where avoidable.

### D-102 · 2026-09-24 · The local control connection, links and the CLI preferring the app (M12-07)
**Decision:** A new crate `crates/control` (no core dependency) serves newline-delimited JSON-RPC 2.0, protocol version 1, on a Unix socket `<data>/control/sock` (0600 in a 0700 folder, peer uid checked) or a Windows named pipe (DACL for the current user only, remote clients refused, first instance; its name in `<data>/control/pipe`). Nothing listens on TCP. The first request must be `hello` with the token from `<data>/control/token` (32 random bytes, 0600, compared in constant time) within 5 s. Limits: 1 MiB per message, 40-request burst at 20/s, 12 changes a minute, 8 in flight, 32 connections. Methods: `status`, `shares.list/start/stop`, `routes.list/preview/apply` (by fingerprint, recorded in Activity with `actor.via = "control"`), `open`, `doctor.run`, `events.subscribe` (`sharesChanged`, `routesChanged`, `requestArrived`; clients ignore unknown types). Changes ask in the app (Allow / Always Allow / Don't Allow per client name); changes to records Teitunnel didn't create ask every time and never offer Always. `teitunnel://` links (`share?port=`, `open`, `open?route=`, `open?share=`, `inspect?share=`) are parsed strictly; a share from a link always asks, one dialog at a time, is never remembered, and links can be turned off; the scheme is registered by the installers, never at runtime (`register_all` would run `xdg-mime`). The CLI uses the running app for `share`, `shares` and `routes` (`--app` requires it, `--here` opts out; `routes --check` stays local), which changes D-059: a terminal share now lives in the app when it runs. Live completion goes through a hidden `teitunnel __complete` (clap's dynamic completion is still unstable) that reads the database read-only and never goes online; `completions --static` prints clap's script. `teitunnel top` is a ratatui 0.30 dashboard (shares, routes, traffic, requests once the inspector reports them; NO_COLOR, compact layout). `cargo deny` allows 0BSD (two small crates under `interprocess`).
**Why:** One small, stable contract for the CLI, editors and launchers without turning the app into a web server; a link or an extension must never share something without the person saying so.

### D-103 · 2026-09-24 · Owners in DNS comments; reservations are placeholder records (M12-11)
**Decision:** Teitunnel's DNS comment becomes `teitunnel:route=<id>[;by=<owner>][;lease][;until=YYYY-MM-DDTHH:MMZ]` (within Cloudflare's 100 characters; the old `teitunnel:route=<id>` still parses). The owner is `$USER@machine` or `TEITUNNEL_OWNER`. A reservation is a proxied `AAAA 100::` record at the hostname with comment `teitunnel:lease;by=…;until=…`: it sits on the name in the dashboard, actually occupies it (Cloudflare refuses a conflicting CNAME), serves nothing (`100::/64` is the discard prefix, RFC 6666; Cloudflare's documented originless pattern) and costs one record. The observe step computes which names are held (another machine's route, someone else's live lease); adding, renaming, importing, reserving or publishing a Snapshot on a held name gives `Warning::HeldBy` and needs confirmation (`--take-over` in the CLI, exit code 3 without it); shares never take a name. A route on your own reserved name keeps the lease and removing it puts the placeholder back; ended leases are free; a record Teitunnel didn't write is never reserved. The same owner through another tunnel (an earlier CI run) counts as yours. Migration 14 caches the account's reservations for the UI. Behaviour change: adding a route another machine already serves now asks instead of silently repointing it, so load balancing by adding the same route on each machine needs the confirmation.
**Why:** Teams share one account; the name is the contract, so the claim lives in DNS where every machine and the dashboard see it, without a server of our own.

### D-104 · 2026-09-24 · Pull request previews: a dependency-free JavaScript Action and a GitLab template (M12-11)
**Decision:** `integrations/github-action/` is a JavaScript action on Node 24 with no dependencies or build step (only JavaScript and Docker actions get a `post` step, which is what cleans up after a failed job). It installs the released CLI checked against `SHA256SUMS.txt`, passes the token only through the environment, and has three modes: `snapshot` (default, `snapshot publish --or-update`), `share` (a per-job tunnel, `share --on … --json`, deleted in `post`) and `cleanup` (`snapshot rm --missing-ok`, for `pull_request: closed`). Hostnames come from templates (`{number}`, `{branch}`, `{sha}`, `{repo}`, `{owner}`, `{zone}`); one comment per hostname is edited in place, and a failed comment (a fork's read-only token) is only a warning. `integrations/gitlab-ci/teitunnel.gitlab-ci.yml` offers hidden jobs using environments and `on_stop`. The CLI gained `share --json`, `share --on` running its own connector when nothing serves the tunnel, `snapshot publish --or-update`, `snapshot rm --missing-ok`, `cloudflared status|install`, `TEITUNNEL_MACHINE_NAME`, and adoption of Snapshots published elsewhere. The Action is published from a separate repo (`teispace/teitunnel-action`) by the maintainer, after a CLI release with these changes.
**Why:** Previews per pull request on your own domain are what Vercel-style workflows expect; a post step is the only reliable cleanup.

### D-105 · 2026-09-24 · Edge rules scoped to one hostname (M12-04)
**Decision:** Teitunnel writes Cloudflare rules one at a time under the phase entry points (`http_request_firewall_custom`, `http_ratelimit`, `http_request_late_transform`, `http_response_headers_transform`; `POST /zones/{z}/rulesets` for the first rule), never `PUT …/entrypoint`, which would replace everyone's rules. Every rule is scoped `(http.host eq "<hostname>")` and marked `teitunnel:<route-id>:<kind>` in its description, with a local index (migration 15) as a backup; foreign rules are counted and never touched. One rule per kind per hostname; rate limits are one shared rule per limit per zone (`http.host in {…}`), and a different limit on a full quota is refused with the hostnames holding it. **A Free zone's rate limit can't match a hostname** (only path and verified bot), so rate limiting is offered from Pro up and refused on Free; Bot Fight Mode and "Block AI bots" are domain-wide and not offered. "Bots" means not a verified bot plus an automated user agent, matched without regex (bot score is Enterprise). Quotas come from the zone's `plan.legacy_id` (no quota API; unknown plans count as Free) and plans show usage after the change. Removals run from the end of each phase so undo restores positions. Permission template keys `zone_waf`, `transform_rules`, `access_service_tokens` and their OAuth scopes are unverified; the capability probes and the in-place fix cover a dropped key. Research: `docs/research/cloudflare-edge-rules.md`.
**Why:** Maintainer choice (hostname-scoped only); a tool that shares one hostname must never change a whole domain's behaviour.

### D-106 · 2026-09-24 · Access service tokens for machines, secrets never stored
**Decision:** A service token for a hostname is added to a "Machines" (Service Auth, `decision: "non_identity"`) policy on Teitunnel's Access application; a hostname without a login gets a machine-only application, with a `MachineOnly` warning. Editing who may log in keeps the Machines policy, and removing a route's login keeps a machine-only app while tokens use it. The secret is never stored anywhere, not even the keychain (this replaces the plan's wording): the app keeps it in memory for 10 minutes and copies it from Rust (it never crosses IPC), the CLI prints it once, and an approved agent gets it once marked sensitive. Only the token id and client id are kept (migration 15). Create, rotate and revoke go through plan → apply.
**Why:** Cloudflare shows the secret only once; keeping a copy would make Teitunnel a store of long-lived machine credentials.

### D-107 · 2026-09-24 · Project files (`teitunnel.yml`) (M12-12)
**Decision:** A versioned YAML file (`version: 1`) with `project`, `account`, `routes`, `shares`, `snapshots` and `localDomains`, parsed into a spanned tree (`serde_saphyr::Spanned`) so every problem has a line and column, and checked with core's own validators. Unknown or future keys warn instead of failing. Secrets are only `{ env: … }` or `{ keychain: … }` references; a literal under a secret-named key, or any credential-looking value anywhere (Cloudflare, tunnel, JWT, private key, GitHub, Stripe, Slack, AWS), is refused. Placeholders `{branch}` (read from `.git/HEAD`, worktrees followed, no git process), `{user}` and `{project}` become DNS labels. Applying builds one combined preview; each route is re-planned just before it is applied and refused if its steps no longer match what was shown; a second run changes nothing. Routes a project created are recorded in the settings table (no migration), so `down --remove-routes` removes only those. `teitunnel up` applies a file found in the folder first. The JSON Schema is published at `/schema/teitunnel.v1.json` and a test keeps it in sync with the parser. The app lists projects (sidebar, no ⌘-digit) and offers to re-apply when the file changes.
**Why:** Config as code for teams and CI, with the same reviewed plans as the app, and no way to commit a secret into it.

### D-108 · 2026-09-24 · The exposure check before sharing
**Decision:** Before a share, `share --on`, `route add` or a project's new routes, Teitunnel probes the local service directly (never through the edge): about 20 GETs plus one raw TCP request, 64 KiB per answer, no redirects, a 2 s deadline with partial results kept. Findings come from content, never status alone (so a single-page app answering every path isn't flagged): `.env` (variable names only), `.git`, directory listings, dumps, debug pages (Django, Laravel, Rails, Symfony, Spring actuator, phpinfo, stack traces), open admin tools, a database on the port, Jupyter without a token, source maps with sources. It never blocks: "Share Anyway" in the app, a warning in the CLI unless `--strict`. On by default (Settings ▸ General ▸ Before Sharing). A CLI share handed to the running app is checked in the CLI first.
**Why:** The most damaging mistake when sharing a dev server is exposing secrets that sit next to it; a fast, local check catches the common ones without slowing the flow.

### D-109 · 2026-09-24 · Encrypted backups for moving to a new computer
**Decision:** A backup is a 70-byte authenticated header (magic, format, argon2id parameters, salt, 24-byte nonce) and gzipped JSON sealed with XChaCha20-Poly1305; the key comes from argon2id (64 MiB, 3 passes), with parameters capped when reading so a crafted file can't exhaust memory; files are written 0600. It holds settings, local tunnels (metrics port cleared, Always-on reset to session), the DNS, Access and edge-rule ownership indexes, service token ids (never secrets), balanced routes, Snapshots and their versions, and accounts by name only. Never included: tokens, OAuth grants, run tokens, webhook secrets, CA keys, dashboard password or API key hashes (a test scans the plaintext). Restore shows what it brings and replaces, runs in one transaction limited to columns that exist here, and refuses a newer schema; accounts are connected again afterwards.
**Why:** Moving machines shouldn't mean rebuilding ownership by hand, and a backup file must be safe to leave in a downloads folder.

### D-110 · 2026-09-24 · The inspector in the app, CLI, serve and MCP (M12-02 integration)
**Decision:** One `Inspector` (Lens) per process, started on first use. Quick Shares go through a tap by default (setting `inspectQuickShares`, per-share toggle; toggling restarts with a new URL), and the tap sets the Host header, so the dev-server fix (D-096) applies live without a new address. Routes and shares on your domain are inspected by a reviewed `UpdateRoute` plan that points the route at the tap; the original service is recorded in `inspected_routes` (migration 16) before the apply and restored when inspection ends, when its process quits, at the next launch after a crash, when a CLI owner exits (swept every 30 s by the app, and by `up`/`serve`), and by the Doctor's `inspect.orphan` fix. Captures persist to SQLite already masked (24 h default, 5,000 per tap, 256 MB), so `teitunnel.db` still holds no secrets; masked credentials can't be revealed later and replays from history drop them. `teitunnel traffic` reads that shared history (behind a `Traffic` trait, so a live source over the control connection can come later); `serve` exposes it read-only at `/api/traffic`; MCP's traffic tools use the process's Lens. Presets: `share --mcp --on` (probes Streamable HTTP/SSE, bearer-gated tap, stream keep-alive, client configs) and `share --ai` (Ollama, LM Studio, vLLM behind a bearer). Bearer tokens come from the OS generator, live in the keychain per hostname, are shown to the person once or with `teitunnel token`, and agents only ever see `<TOKEN>`. Lens types that clash with app types in IPC are mirrored in core (`WebhookSender`, `WebhookVerdict`, `TrafficFormat`, `TapProtectionView`), and the edge rules' header operation is `EdgeHeaderOp`. A CLI share that sets inspector options (`--no-inspect`, `--idle`, `--watch`) stays in the terminal instead of going to the app; a project's `inspect:` is honoured.
**Why:** Maintainer decisions Q1 and Q2; every Cloudflare change stays in plan → apply, and inspection can never leave a route pointing at a proxy that's gone.

### D-111 · 2026-09-25 · Inspector screens (M12-02 UI)
**Decision:** The Inspector view (sidebar after Quick Share, no ⌘-digit since all nine are taken; `teitunnel://inspect?share=` opens it) keeps its live list in a store outside React: the first page comes from `inspect_exchanges`, then `inspect_subscribe` batches merge in, newest first, capped at 10,000 rows and virtualised with fixed rows like the log viewer (D-051). Filters (status class, method, path/host, duration) run in the UI with the inspector's rules; full-text search runs in the inspector and new traffic re-runs it. Revealed requests are read with a direct call and kept only in the detail pane's state, never in a query cache; unredacted exports have `gcTime: 0`. Edit and Replay sends only what changed, so masked values are never sent back. Tap protection says "on this computer" and points routes to edge protection. The Quick Share switch confirms the new address first. Inspecting a route is a reviewed plan wherever it's offered.
**Why:** 10,000 rows at 60 fps, and no secret lives in the webview longer than the person asked for.

### D-112 · 2026-09-25 · Editor and launcher integrations on the control connection (M12-07)
**Decision:** `integrations/control-client` is a dependency-free TypeScript client (Node 20+, Unix socket or Windows pipe, hello, typed methods, subscriptions, reconnect with backoff; the token is never logged), bundled into the VS Code extension and vendored into Raycast (a test fails if the copy is stale). VS Code runs on the UI side (`extensionKind: ["ui"]`, Open VSX-compatible) with a status bar item, a Shares/Routes view, share commands, Doctor, opt-in request notifications, and "Share with Teitunnel" on the Ports view's context menu (its data API is still proposed). Raycast stays outside the pnpm workspace (npm and Prettier are Store requirements) and, like the JetBrains plugin, outside Biome. The JetBrains plugin's protocol layer uses only the JDK; its Windows pipe uses `AsynchronousFileChannel` (a synchronous pipe handle blocks writes while a read is pending). The real server's `crates/control/examples/fake_app` backs cross-language interop tests, including the first Windows pipe test in CI (`integrations-test.yml`). Publishing to the VS Code Marketplace, Open VSX, Raycast Store and JetBrains Marketplace is the maintainer's.
**Why:** One small client and one protocol for every editor and launcher; the app stays the only place that changes anything.

### D-113 · 2026-09-25 · `requestArrived` from the inspector
**Decision:** The control connection sends one event per finished, non-replay request, with the path masked like the inspector's list and the share's id (or the hostname for an inspected route). At most 10 events per 250 ms (newest kept, repeats merged); nothing is collected while no client subscribes; Lens is never started just for it.
**Why:** Extensions can show requests without polling, and a burst can't flood a client.

### D-114 · 2026-09-25 · One-click sharing from the menu bar and a global shortcut
**Decision:** The tray offers "Share a Running Service" (discovered, not yet shared), Copy for the three newest live shares, Pause/Resume All (inspected shares, through the tap's paused page) and Stop All. A one-click share runs the exposure check (D-108) first and opens the Quick Share sheet if it finds something; otherwise it copies the address and notifies. Services are looked up when the pointer enters the icon (at most every 3 s; on Linux, which has no tray events, at launch and when shares change). The global shortcut (tauri-plugin-global-shortcut) is off by default, recorded by typing it in Settings ▸ Integrations, shares the one running dev server or opens Quick Share; a refused shortcut (taken, or Wayland) restores the previous one and says why. No approval dialog: the person clicked or pressed the keys themselves.
**Why:** Sharing the thing you're working on should be one gesture, without skipping the safety check.
